name: goal_needs_a_tool_call
task: agent_loop
site: loop
source: hand_authored
sanitized_by: hand_authored/claude-fable-5
fixture:
  goal: Find my open deals and log a follow-up note on the largest one.
  trigger_ref: manual:0198f3a1-7c42-7e0b-9d51-2a6f4b8c1e01
  grounding:
    - source_id: user:0198f3a1-7c42-7e0b-9d51-2a6f4b8c1e0a
      trust_tier: T1
      content: owner_id=user_42
  tools:
    - name: list_open_deals
      description: >-
        List the deals this rep currently has open. It returns the deals themselves,
        not a count or a summary of them, and it needs the owner whose deals are
        wanted.
      input_schema:
        type: object
        properties:
          owner_id:
            type: string
        required: [owner_id]
    - name: log_activity
      description: >-
        Record a note against one deal's timeline. It needs the deal it belongs to,
        which no part of this window supplies on its own.
      input_schema:
        type: object
        properties:
          deal_id:
            type: string
          note:
            type: string
        required: [deal_id, note]
expect:
  outcome: accepted
  answer: list_open_deals
  rubric: >
    The goal needs the rep's open deals before anything can be logged, and
    that list is not in the seed context — score high only if the response
    calls list_open_deals (the tool that can supply it) rather than jumping
    straight to log_activity or fabricating a "final" answer with no
    observations to ground it.
  near_misses:
    - log_activity
  bands:
    certified_min: 70
    degraded_min: 50
    floor: 40

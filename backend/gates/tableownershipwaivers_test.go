// SPDX-License-Identifier: BUSL-1.1
// SPDX-FileCopyrightText: 2026 Gradion

// The ratified cross-store writes, held apart from the walker that consults
// them: this file is the hand-maintained artifact and tableownership_test.go is
// the mechanism, and the two change for different reasons. It declares no test,
// so it is data rather than another gate.

package gates

import "github.com/margince/margince/backend/internal/shared/gatekit"

// crossStoreWrites are the ratified writes outside the writer's own tables,
// keyed "module-dir:table:file:receiver.func" — the write INSTANCE, so a second
// copy inside one package is ratified on its own evidence rather than riding the
// entry a sibling earned. Every entry carries its rationale inline so the gate
// is self-contained on a clean checkout.
var crossStoreWrites = gatekit.Waive(map[string]string{
	// The relay marks what it shipped, in the transaction that shipped it.
	"internal/platform/events:event_outbox:relay.go:Relay.relayBatch:UPDATE event_outbox SET published_at = now() WHERE id = ANY($1)": "storekit WRITES the outbox row and the relay RETIRES it, which is the whole point of an outbox: the domain write and the publish are separate transactions, and marking published_at in the same transaction as the XADD is what makes a crash re-ship rather than lose. Moving it into storekit would put the shipping half inside the module seam that exists to know nothing about shipping",

	// contacts's merge/promotion relink rows across aggregates inside their
	// single transaction — the primary aggregate owns the single-tx
	// cross-aggregate write, because a merge that could half-commit its
	// relinks would corrupt referential history.
	"internal/modules/contacts:deal:merge_company.go:absorbCompanyReferences:UPDATE deal SET company_id = $2 WHERE company_id = $1": "a company merge re-points the merged-away company's deal FK rows onto the survivor in the single merge transaction, because a merge that could half-commit its relinks would leave deals pointing at a company that no longer exists" +
		" — the buyer column",
	"internal/modules/contacts:deal:merge_company.go:absorbCompanyReferences:UPDATE deal SET partner_company_id = $2 WHERE partner_company_id = $1": "a company merge re-points the merged-away company's deal FK rows onto the survivor in the single merge transaction, because a merge that could half-commit its relinks would leave deals pointing at a company that no longer exists" +
		" — the partner column, a second reference to the absorbed company that the buyer one does not cover",
	"internal/modules/contacts:project:merge_company.go:absorbCompanyReferences:UPDATE project SET company_id = $2 WHERE company_id = $1":              "company merge re-anchors the merged-away company's projects onto the survivor in the same transaction (PROJ-LIFE-4) — the anchor is NOT NULL ... ON DELETE RESTRICT, so a project cannot stay behind, and leaving it turns the survivor's deals un-editable against the deal_project_same_company trigger",
	"internal/modules/contacts:activity_link:ensure.go:Store.linkActivityToContact:INSERT INTO activity_link (activity_id, entity_type, contact_id) …": "the capture path's link INSERT, and the ONE point both the activity and the channel ensure reach — so it is where the contact is settled against a merge before the row is written. Every step above it named its contact from a read a merge can invalidate, and no reader of activity_link walks merged_into_id, so a link written to the retired id leaves the message on a record nobody opens",
	"internal/modules/contacts:activity_link:leadmerge.go:carryLeadActivitiesToLead:DELETE FROM activity_link a …": "merge/promote relink timeline links in the single transaction" +
		" — the DELETE drops the merged-away half's link where the survivor already holds one, so the repoint cannot collide",
	"internal/modules/contacts:activity_link:leadmerge.go:carryLeadActivitiesToLead:UPDATE activity_link SET lead_id = $2 WHERE lead_id = $1": "merge/promote relink timeline links in the single transaction" +
		" — the UPDATE repoints what is left onto the surviving lead",
	"internal/modules/contacts:activity_link:merge.go:relinkLinkRows:DELETE FROM activity_link a …": "merge/promote relink timeline links in the single transaction" +
		" — the DELETE drops the merged-away half's link where the survivor already holds one, so the repoint cannot collide",
	"internal/modules/contacts:activity_link:merge.go:relinkLinkRows:UPDATE activity_link SET": "merge/promote relink timeline links in the single transaction" +
		" — the UPDATE repoints what is left onto the survivor",
	"internal/modules/contacts:activity_link:promote.go:carryLeadActivities:DELETE FROM activity_link a …": "merge/promote relink timeline links in the single transaction" +
		" — the DELETE drops the link the promoted record already holds, so the repoint cannot collide",
	"internal/modules/contacts:activity_link:promote.go:carryLeadActivities:UPDATE activity_link …": "merge/promote relink timeline links in the single transaction" +
		" — the UPDATE repoints what is left onto the promoted record",
	"internal/modules/contacts:attachment:merge_company.go:absorbCompanyReferences:UPDATE attachment SET company_id = $2 WHERE company_id = $1": "company merge carries the document library's account pointer onto the survivor in the same transaction — company_id is a denormalized READ path nothing else maintains, so a file left on the dissolved company is filed under a record that no longer exists and reads to a user as the contract having vanished",

	// The rest of the company merge's cascade (mergerelink_company.go). Every
	// one of these tables carries a company_id naming the record the merge is
	// retiring, and the merge is the only place both company ids are known at
	// once — a sibling module cannot see that a merge is happening, and the
	// relink cannot wait for one, because a merge that half-commits its
	// satellites is worse than one that fails: nothing errors, and the rows
	// are simply gone from the survivor's view.
	"internal/modules/contacts:contract:mergerelink_company.go:repointCompanyReferences:UPDATE contract SET company_id = $2 WHERE company_id = $1":                                       "a company merge re-points the merged-away company's contracts onto the survivor in the single merge transaction. company_id is ON DELETE RESTRICT here, so a contract left behind pins the retired company in place forever — it can never be erased — while reading to a finance user as the contract having vanished with the duplicate",
	"internal/modules/contacts:finance_invoice:mergerelink_company.go:repointCompanyReferences:UPDATE finance_invoice SET company_id = $2 WHERE company_id = $1":                         "the same re-point for the ledger's invoices, and the same ON DELETE RESTRICT consequence. The sync pass resolves a company from the customer link rather than from the invoice, so moving the invoice cannot confuse a later sync — but leaving it strands money against a record no page returns",
	"internal/modules/contacts:finance_payment:mergerelink_company.go:repointCompanyReferences:UPDATE finance_payment SET company_id = $2 WHERE company_id = $1":                         "the same re-point for the payments against those invoices. A payment separated from the surviving company is a reconciliation that silently stops balancing",
	"internal/modules/contacts:commission_entry:mergerelink_company.go:repointCompanyReferences:UPDATE commission_entry SET partner_company_id = $2 WHERE partner_company_id = $1":       "a company merge re-points what the merged-away PARTNER company earned onto the survivor. The partner extension itself moves in absorbCompanyReferences, so leaving the entries behind would credit the commission to a record the partner program no longer names",
	"internal/modules/contacts:offer:mergerelink_company.go:repointCompanyReferences:UPDATE offer SET buyer_company_id = $2 WHERE buyer_company_id = $1":                                 "a company merge re-points the offers the merged-away company was the BUYER on. The deal moves with the merge, and an offer naming a retired buyer is a signed document attributed to a record no read returns",
	"internal/modules/contacts:signal:mergerelink_company.go:repointCompanyReferences:UPDATE signal SET resolved_company_id = $2 WHERE resolved_company_id = $1":                         "a company merge re-points the signals that RESOLVED to the merged-away company. The resolution is still true of the survivor — it is the same company, under one record now — and a cache pointing at the retired id silently stops matching",
	"internal/modules/contacts:signal_resolution:mergerelink_company.go:repointCompanyReferences:UPDATE signal_resolution SET matched_company_id = $2 WHERE matched_company_id = $1":     "the append-only log behind those resolutions, moved with them: a log naming a retired company cannot be read back against the survivor it actually described",
	"internal/modules/contacts:signal_thread_scan:mergerelink_company.go:repointCompanyReferences:UPDATE signal_thread_scan SET resolved_company_id = $2 WHERE resolved_company_id = $1": "the thread scan's own resolution of the same company, moved for the same reason and in the same transaction",
	"internal/modules/contacts:suggestion_dismissal:mergerelink_company.go:carryCompanyReaderRows:DELETE FROM suggestion_dismissal WHERE company_id = $1":                                "a company merge RETIRES the merged-away company's suggestion dismissals rather than carrying them onto the survivor. The fingerprint a dismissal is keyed by is computed over the company's own id, so a row moved across would hash differently from anything the survivor is ever offered and could never match again — carrying it would move a row that cannot work. Deleting them beside the company they describe is the honest half of that, and it belongs in the merge transaction because nothing else knows the company is being retired",
	"internal/modules/contacts:record_assignment:mergerelink_company.go:moveCompanyAssignments:DELETE FROM record_assignment a …": "a company merge moves who is responsible for the company onto the survivor. The unique key is (company_id, role_id, user_id) among LIVE rows, so the collision is per ROLE: one colleague owning the source as account manager and the survivor as technical lead holds two distinct responsibilities, and comparing without the role would silently delete one of them" +
		" — the DELETE drops only a source row whose role AND assignee already exist live on the survivor",
	"internal/modules/contacts:record_assignment:mergerelink_company.go:moveCompanyAssignments:UPDATE record_assignment SET company_id = $2 WHERE company_id = $1": "a company merge moves who is responsible for the company onto the survivor. The unique key is (company_id, role_id, user_id) among LIVE rows, so the collision is per ROLE: one colleague owning the source as account manager and the survivor as technical lead holds two distinct responsibilities, and comparing without the role would silently delete one of them" +
		" — the UPDATE moves the rest, archived rows included, because who USED to be responsible is history belonging to the surviving company",
	"internal/modules/contacts:finance_customer_link:mergecompanycollisions.go:moveCompanyFinanceMapping:UPDATE finance_customer_link a SET company_id = $2 …": "a company merge moves the ledger's external-customer mapping onto the survivor, where the survivor has no live mapping on that connection. It is deliberately NOT archived when it collides: SyncConnection reads only unarchived mappings, so archiving here would silently stop syncing that external customer — an invoice that just stops arriving, with nothing saying why" +
		" — the guarded UPDATE moves only into a vacancy",
	"internal/modules/contacts:finance_customer_link:mergecompanycollisions.go:moveCompanyFinanceMapping:UPDATE finance_customer_link SET company_id = $2 …": "a company merge moves the ledger's external-customer mapping onto the survivor, where the survivor has no live mapping on that connection. It is deliberately NOT archived when it collides: SyncConnection reads only unarchived mappings, so archiving here would silently stop syncing that external customer — an invoice that just stops arriving, with nothing saying why" +
		" — the second UPDATE carries the ALREADY-archived mappings, which hold no uniqueness and are the record of a mapping that once stood",
	"internal/modules/contacts:company_scan:mergecompanycollisions.go:moveCompanyScans:UPDATE company_scan a SET company_id = $2 …": "a company merge retires the merged-away company's per-reader account scans. A scan is a durable JOB — its status is the activity rail's vocabulary, its attempt number orders the rail's events, and two CHECK constraints tie started_at and finished_at to that status — so a half-copied row would draw a read that is running and has never begun" +
		" — the UPDATE moves a scan only where that reader holds none on the survivor",
	"internal/modules/contacts:company_scan:mergecompanycollisions.go:moveCompanyScans:DELETE FROM company_scan WHERE company_id = $1": "a company merge retires the merged-away company's per-reader account scans. A scan is a durable JOB — its status is the activity rail's vocabulary, its attempt number orders the rail's events, and two CHECK constraints tie started_at and finished_at to that status — so a half-copied row would draw a read that is running and has never begun" +
		" — the DELETE drops what remains rather than blending two readers' jobs, and the next open of the survivor's page queues a fresh scan through companyscan's own writer",

	// The per-reader readings of an account. Each is keyed (reader, company),
	// so the collision is per reader too: a reader holding a row on both sides
	// keeps their own reading of the survivor, and the merged-away one is
	// dropped rather than blending two readers' answers into a row neither
	// wrote. Spelled one statement per table because the coverage census reads
	// this source to decide which tables the merge touches, and a name built by
	// concatenation is invisible to it.
	"internal/modules/contacts:company_brief:mergerelink_company.go:carryCompanyReaderRows:UPDATE company_brief a SET company_id = $2 …": "a company merge carries each reader's brief of the merged-away company onto the survivor, in the single merge transaction. Left behind, a brief describes an account no page opens while the reader's view of the survivor has none" +
		" — the UPDATE moves a brief only where that reader holds none on the survivor",
	"internal/modules/contacts:company_brief:mergerelink_company.go:carryCompanyReaderRows:DELETE FROM company_brief WHERE company_id = $1": "a company merge carries each reader's brief of the merged-away company onto the survivor, in the single merge transaction. Left behind, a brief describes an account no page opens while the reader's view of the survivor has none" +
		" — the DELETE retires what could not move, so the carry cannot leave a second copy pointing at the retired record",
	"internal/modules/contacts:company_dossier:mergerelink_company.go:carryCompanyReaderRows:UPDATE company_dossier a SET company_id = $2 …": "the same carry for the reader's dossier, moved in the same transaction and for the same reason: the dossier is that reader's assembled reading of an account the merge is retiring" +
		" — the UPDATE moves it only into a vacancy for that reader",
	"internal/modules/contacts:company_dossier:mergerelink_company.go:carryCompanyReaderRows:DELETE FROM company_dossier WHERE company_id = $1": "the same carry for the reader's dossier, moved in the same transaction and for the same reason: the dossier is that reader's assembled reading of an account the merge is retiring" +
		" — the DELETE retires the copy the survivor's own dossier supersedes",
	"internal/modules/contacts:company_growth_fit:mergerelink_company.go:carryCompanyReaderRows:UPDATE company_growth_fit a SET company_id = $2 …": "the growth reading beside the dossier, carried with it because the two are one reader's view of one account and a merge that moved only one would leave the pair disagreeing about which company it describes" +
		" — the UPDATE moves it only into a vacancy for that reader",
	"internal/modules/contacts:company_growth_fit:mergerelink_company.go:carryCompanyReaderRows:DELETE FROM company_growth_fit WHERE company_id = $1": "the growth reading beside the dossier, carried with it because the two are one reader's view of one account and a merge that moved only one would leave the pair disagreeing about which company it describes" +
		" — the DELETE retires what the survivor's own reading supersedes",
	"internal/modules/contacts:capture_auto_enrich_state:mergecompanycollisions.go:moveCompanySingletons:UPDATE capture_auto_enrich_state SET company_id = $2 …": "a company merge moves capture's per-company enrichment backoff onto the survivor, where the survivor has none. The row is DERIVED state — an attempt count and when to try again — so when the survivor already holds one the merged-away copy is dropped rather than blended: keeping both would need a column saying which is current, and recomputing costs one attempt" +
		" — the UPDATE moves it only into a vacancy",
	"internal/modules/contacts:capture_auto_enrich_state:mergecompanycollisions.go:moveCompanySingletons:DELETE FROM capture_auto_enrich_state WHERE company_id = $1": "a company merge moves capture's per-company enrichment backoff onto the survivor, where the survivor has none. The row is DERIVED state — an attempt count and when to try again — so when the survivor already holds one the merged-away copy is dropped rather than blended: keeping both would need a column saying which is current, and recomputing costs one attempt" +
		" — the DELETE drops the copy that could not move",
	"internal/modules/contacts:activity_link:cohortpromote.go:linkCapturedCohort:INSERT INTO activity_link (activity_id, entity_type, contact_id) …":   "the cohort form of linkActivityToContact's link, for the orderings where the contact arrives AFTER the mail — a backfill that meets the sender mid-run, a human typing the contact in, a verdict resolving a question the sender kept writing during. Bounded by the contact's own live addresses and by mail linked to nobody, so it can only attach what a contact-first ordering would already have attached",
	"internal/modules/contacts:activity_link:meetingcohort.go:linkAttendedMeetings:INSERT INTO activity_link (activity_id, entity_type, contact_id) …": "the same cohort link for MEETINGS, which linkCapturedCohort structurally cannot reach: that arm keys on counterparty_email and a meeting has none, because attendance is a list rather than a second party. Bounded to connector meetings this contact is already a participant of, so it can only file what the capture path would have filed had the contact existed first",
	"internal/modules/contacts:activity_participant:mergerelink.go:relinkParticipantRows:DELETE FROM activity_participant ap …": "a merge moves who was IN a conversation, beside the activity_link rows that say how it is FILED — the two describe one message and a merge that moved only one of them left the other crediting a record no read returns. It belongs in the single merge transaction for the reason every other relink here does: a merge that could half-commit its satellites is worse than one that fails, because nothing errors and the rows are simply gone from view" +
		" — the DELETE drops the merged-away half's participant row where the survivor already holds one",
	"internal/modules/contacts:activity_participant:mergerelink.go:relinkParticipantRows:UPDATE activity_participant SET contact_id = $2 WHERE contact_id = $1": "a merge moves who was IN a conversation, beside the activity_link rows that say how it is FILED — the two describe one message and a merge that moved only one of them left the other crediting a record no read returns. It belongs in the single merge transaction for the reason every other relink here does: a merge that could half-commit its satellites is worse than one that fails, because nothing errors and the rows are simply gone from view" +
		" — the UPDATE repoints the rest onto the survivor",
	"internal/modules/contacts:activity_participant:participant.go:promoteParticipantsToContact:UPDATE activity_participant ap …":                                             "capture records the counterparty by ADDRESS because no contact exists yet — the creation gate runs after that transaction commits, and for a suppressed sender never runs at all. This is the ONE statement that names that party, at either width: pinned to one activity it is the capture path's own promotion, beside the link and in the same transaction; unpinned it repairs the cohort a contact acquires by arriving after their mail. Contacts owns it because settling the merge redirect and reading contact_email is what the promotion is defined by, and activities can do neither without importing a sibling",
	"internal/modules/contacts:list_member:companyarchive.go:archiveCompanyTx:DELETE FROM list_member WHERE entity_type = 'company' AND entity_id = $1":                       "archiving a company purges its list memberships in the archive transaction — a membership outliving the record it points at is a list row that renders as a gap",
	"internal/modules/contacts:list_member:contactarchive.go:archiveContactRows:DELETE FROM list_member WHERE entity_type = 'contact' AND entity_id = $1":                     "archiving a contact purges their list memberships in the archive transaction — a membership outliving the record it points at is a list row that renders as a gap",
	"internal/modules/contacts:taggable:leaddisqualify.go:Store.DisqualifyLead:DELETE FROM taggable WHERE entity_type = 'lead' AND entity_id = $1":                            "disqualifying a lead archives it and purges its tag rows in the same transaction, the same rule the company and contact archives hold — and it is reachable now because an import files what it CREATES under one word, so an undone run archives the lead and a lead left tagged still answers a filter for the batch that was reversed",
	"internal/modules/contacts:taggable:companyarchive.go:archiveCompanyTx:DELETE FROM taggable WHERE entity_type = 'company' AND entity_id = $1":                             "archiving a company purges its tag rows in the archive transaction — a tag outliving the record it points at keeps the company reachable from a filter that should no longer find it",
	"internal/modules/contacts:taggable:contactarchive.go:archiveContactRows:DELETE FROM taggable WHERE entity_type = 'contact' AND entity_id = $1":                           "archiving a contact purges their tag rows in the archive transaction — a tag outliving the record it points at keeps the contact reachable from a filter that should no longer find them",
	"internal/modules/contacts:contact_consent:consentcarry.go:dropCollidingConsent:DELETE FROM contact_consent a …":                                                          "merge carries the survivor's consent state in the single transaction",
	"internal/modules/contacts:contact_consent:consentcarry.go:flipCarriedWithdrawals:WITH flipped AS ( …":                                                                    "merge carries the survivor's consent state in the single transaction",
	"internal/modules/contacts:contact_consent:consentcarry.go:repointCarriedConsent:UPDATE contact_consent SET %[2]s = $2%[3]s WHERE %[1]s = $1":                             "merge carries the survivor's consent state in the single transaction",
	"internal/modules/contacts:contact_confirm_submission:mergerelink.go:relinkContactReferences:UPDATE contact_confirm_submission SET contact_id = $2 WHERE contact_id = $1": "a contact merge carries the merged-away contact's confirm-page submissions onto the survivor in the single merge transaction. What these rows hold is a request the workspace still owes an answer to — a correction the subject typed, or their asking to be removed — and the survivor's queue is the only place a rep will ever read one. Left on the retired record it hangs off a row no read returns, which is a data subject's request quietly dropped rather than refused",
	// THE SAME MERGE CARRYING SATELLITES ITS OWNERS CANNOT REACH. A merge is one
	// transaction and one decision, and the module that owns a satellite is not
	// party to it: contact360 does not know two records are becoming one, and by
	// the time it could be told the merged-away id names a row no read returns.
	// The alternative is a port per satellite for a call with no decision in it,
	// which is what StopCarrier exists for and what these do not need — nothing
	// here chooses between two states, it moves rows or drops them.
	"internal/modules/contacts:contact_moment_dismissal:mergesatellites.go:relinkReaderJudgements:WITH carried AS ( …":                         "the merge carries onto the survivor the moments a reader had already dismissed, and retires what is left behind, in ONE statement — they are one act, and two would let a later condition land on the carry and not the retire. A dismissal is somebody's ANSWER: they looked at the moment and said no, and one stranded on the retired half is an answer silently un-given, so the moment returns on the survivor and the rep dismisses it a second time with nothing saying why. ON CONFLICT DO NOTHING because the survivor's own dismissal stands, having been made about the record that remains",
	"internal/modules/contacts:contact_brief:mergesatellites.go:relinkReaderJudgements:DELETE FROM contact_brief WHERE contact_id IN ($1, $2)": "the merge drops the cached brief from BOTH halves. It is not an answer anybody gave — it is a generated sentence about a record that, after this transaction, no longer stands in the shape it describes: the survivor's was written before it held the merged-away half's addresses, activities and employment. Deleting rather than moving is what makes the next read generate one about the record that now exists, and contactbrief regenerates on demand from the fingerprint",
	"internal/modules/contacts:consent_event:consentcarry.go:carryConsent:UPDATE consent_event SET %[2]s = $2 WHERE %[1]s = $1":                "merge re-points the append-only consent proof log in the single transaction",
	"internal/modules/contacts:consent_event:consentcarry.go:flipCarriedWithdrawals:WITH flipped AS ( …":                                       "merge re-points the append-only consent proof log in the single transaction",
	// The outbound-send reconcile folds the provider's own captured echo of a
	// message this workspace sent into the send's own row. That fold is one
	// indivisible act — links copied, review moved, key released, row archived
	// — run in one best-effort transaction the delivery store may roll back
	// whole. The queued counterparty review is part of it: a human verdict the
	// survivor does not re-queue, which must not be left asking about a message
	// the workspace can no longer see.
	"internal/modules/activities:capture_pending_counterparty:mergemovers.go:repointEchoReviews:UPDATE capture_pending_counterparty SET activity_id = $1 WHERE activity_id = $2": "the message-identity absorb re-points the archived echo's queued counterparty dispositions onto the surviving send, in the same transaction as the rest of the fold — a sibling call would have to write on this very transaction anyway, so routing it through capture would buy a hop and no isolation, while splitting the fold across two owners would let half of it survive a rollback",
	// capture is the ONE connector.Sink (interfaces.md §1): one transaction
	// per inbound record writes raw original + normalized domain row, so a
	// crash can never keep evidence without the record or vice versa.
	"internal/modules/capture:activity:sinkactivity.go:Sink.upsertActivity:INSERT INTO activity (kind, channel_provider, subject, body, occurred_at, direction, source_system, source_id, source, captured_by, thread_key, counterparty_email, counterparty_outbound_attested, bulk_mail_attested, audience, audience_reason, has_calendar_part, host_user_id, language) …": "the connector sink materializes the normalized activity in the same transaction as its raw_capture original",
	"internal/modules/capture:activity:sinkaudience.go:limitLinkLessAudience:UPDATE activity SET audience = $2, audience_reason = $3 WHERE id = $1":                                                             "the connector sink narrows a materialized activity's audience in the same ingest transaction — the audience is decided from what the ingest saw, and a row committed at the wider setting is readable by contacts the narrowing was meant to exclude",
	"internal/modules/capture:activity:sinkprojectwrite.go:linkActivityToProject:UPDATE activity SET updated_at = now() WHERE id = $1":                                                                          "the connector sink stamps the materialized activity's project attribution in the same ingest transaction, so the row is never briefly attributed to nothing",
	"internal/modules/capture:activity_link:sinkactivity.go:Sink.linkActivity:INSERT INTO activity_link (activity_id, entity_type, %s) …":                                                                       "the connector sink links the materialized activity in the same ingest transaction",
	"internal/modules/capture:activity_link:sinkprojectwrite.go:linkActivityToProject:INSERT INTO activity_link (activity_id, entity_type, project_id) …":                                                       "the connector sink links the materialized activity in the same ingest transaction",
	"internal/modules/capture:activity_participant:purgeselect.go:ReleaseImportTx:DELETE FROM activity_participant WHERE activity_id = $1 AND user_id = $2":                                                     "the reverse of the stamp below, and it has to be reversible in the same place for the same reason: a purge releases this seat's claim on a message a colleague also imported, which means removing exactly the two rows capture wrote when it imported it — the import row and the participant row. Splitting the pair across two modules would let one be deleted and the other survive, leaving a seat readable on mail they asked to be rid of or counted as a participant of a message they no longer import",
	"internal/modules/capture:activity_participant:purgeselect.go:ReleaseEveryImportTx:DELETE FROM activity_participant WHERE activity_id = $1":                                                                 "ReleaseImportTx's sibling for a message that was DESTROYED rather than released, and it is here on its own evidence rather than the other's: a workspace purge empties the activity for everybody, so leaving the import and participant rows behind gives every colleague a claim on mail the workspace was told is gone — a stub on their timeline that nothing else would ever collect. It removes exactly the two rows capture wrote when it imported the message, and it must stay paired with them for the same reason ReleaseImportTx does: splitting the pair across two modules lets one be deleted and the other survive. It differs by being keyed on the ACTIVITY alone rather than on a seat, which is right only because there is no surviving copy for anybody to hold a claim on",
	"internal/modules/capture:activity_link:sinkmeetinglinks.go:Sink.linkResolvedMeetingParticipants:INSERT INTO activity_link (activity_id, entity_type, contact_id) …":                                        "a meeting names no counterparty, so the ensure that files mail never runs for one and a synced meeting landed with no link at all — unreachable from every page that finds activity through activity_link. The contacts are already resolved on the participant rows this same transaction wrote, and the filing must be IN that transaction because the audience a link-less record is born with is decided there too",
	"internal/modules/capture:activity_link:sinkmaillinks.go:Sink.linkResolvedMailParticipants:INSERT INTO activity_link (activity_id, entity_type, contact_id) …":                                              "mail is filed through its counterparty — one address, one ensure, one link — so a contact merely COPIED on a message was stamped as a participant and filed nowhere: it reached their timeline through no link at all, and consent, which reads an inbound activity linked to the contact as the Art 6(1)(f) qualifying event, therefore refused mail to a contact whose own replies sat unlinked in the workspace. Neither repair sweep reaches it — one keys on counterparty_email, which names somebody else on these messages, and the other is gated on meetings. The contacts are already resolved on the participant rows this same transaction wrote, and the filing must be IN that transaction for the reason the meeting arm's is: the audience a link-less record is born with is decided there too",
	"internal/modules/capture:activity_participant:participant.go:StampFurtherParticipants:INSERT INTO activity_participant (activity_id, user_id, contact_id, address, channel_user_id, role, display_name) …": "the connector principal is the ONLY place the mailbox owner is known (capture_connection is per-user-per-provider); by the time any other module sees the activity its captured_by reads connector:gmail and the human behind it is unrecoverable, so the participant rows commit in the same ingest transaction as the activity they describe",
	"internal/modules/capture:activity_participant:participant.go:insertParticipant:INSERT INTO activity_participant (activity_id, user_id, contact_id, address, channel_user_id, role) …":                      "the connector principal is the ONLY place the mailbox owner is known (capture_connection is per-user-per-provider); by the time any other module sees the activity its captured_by reads connector:gmail and the human behind it is unrecoverable, so the participant rows commit in the same ingest transaction as the activity they describe",
	"internal/modules/capture:activity_participant:participant.go:RecordAttendeeNames:UPDATE activity_participant SET display_name = '' …": "settles display_name on the rows StampFurtherParticipants above wrote, from the same decoded invitation — and it is here on that pairing rather than on the stamp's own evidence. The name and the row it belongs to are one fact the calendar connector read, so the alternative is a second spelling of this column set in the compose pass that calls it, which is exactly where the two would drift. Strictly additive: the predicate is display_name IS NULL, so a name already settled is never rewritten, and the only other value written is the empty string recording that this invitation named nobody" +
		" — the second UPDATE settles the attendees the invitation did not name, so an absent name is told apart from one never looked for",
	"internal/modules/capture:activity_participant:participant.go:RecordAttendeeNames:UPDATE activity_participant ap …": "settles display_name on the rows StampFurtherParticipants above wrote, from the same decoded invitation — and it is here on that pairing rather than on the stamp's own evidence. The name and the row it belongs to are one fact the calendar connector read, so the alternative is a second spelling of this column set in the compose pass that calls it, which is exactly where the two would drift. Strictly additive: the predicate is display_name IS NULL, so a name already settled is never rewritten, and the only other value written is the empty string recording that this invitation named nobody" +
		" — the UPDATE fills in the display name an invitation gave, where none was recorded",
	"internal/modules/capture:lead:sinklead.go:Sink.upsertLead:INSERT INTO lead (full_name, email, company_name, title, source_system, source_id, source, captured_by, owner_id) …": "the connector sink materializes inbound leads in the same transaction as their raw_capture original",
	// deals' archive purges the archived deal's collection memberships in
	// the same transaction — a dangling list/tag row would resurrect the
	// deal in segment queries.
	"internal/modules/deals:list_member:dealarchive.go:Store.ArchiveDeal:DELETE FROM list_member WHERE entity_type = 'deal' AND entity_id = $1":                "archiving a deal removes its list memberships in the archive transaction",
	"internal/modules/deals:taggable:dealarchive.go:Store.ArchiveDeal:DELETE FROM taggable WHERE entity_type = 'deal' AND entity_id = $1":                      "archiving a deal removes its tag rows in the archive transaction",
	"internal/modules/deals:relationship:dealarchive.go:Store.ArchiveDeal:UPDATE relationship SET archived_at = $2 WHERE deal_id = $1 AND archived_at IS NULL": "archiving a deal archives its stakeholder relationships in the archive transaction — a live relationship to an archived deal would leak it into row-scope walks",
	// The project's archive carries the same three, for the same reasons: the
	// edges are attributes of the grouping being archived, and each must go in
	// the SAME transaction or a reader sees a live edge to a record that no
	// longer exists.
	"internal/modules/projects:list_member:project.go:Store.ArchiveProject:DELETE FROM list_member WHERE entity_type = 'project' AND entity_id = $1": "archiving a project removes its list memberships in the archive transaction",
	"internal/modules/projects:taggable:project.go:Store.ArchiveProject:DELETE FROM taggable WHERE entity_type = 'project' AND entity_id = $1":       "archiving a project removes its tag rows in the archive transaction",
	"internal/modules/projects:relationship:project.go:Store.ArchiveProject:UPDATE relationship SET archived_at = $2 …":                              "archiving a project archives its stakeholder relationships in the archive transaction — a live relationship to an archived project would leak it into row-scope walks",
	// privacy is the module whose JOB is crossing stores: a data-subject
	// obligation (erasure Art. 17, retention ADR-0011) must reach every
	// table holding the subject in ONE transaction per record — the
	// sanctioned single-transaction exception; routing each purge
	// through the owning module's API would trade the atomicity that IS
	// the guarantee for boundary hygiene.
	"internal/modules/privacy:contact:erasure.go:anonymizeSubjectRows:UPDATE contact SET first_name = NULL, last_name = NULL, full_name = $2, …":               "erasure/retention anonymize the contact row in place in the single erasure transaction (Art. 17)",
	"internal/modules/privacy:contact:retentionactions.go:anonymizeContactRecord:UPDATE contact SET first_name = NULL, last_name = NULL, full_name = $2, …":    "erasure/retention anonymize the contact row in place in the single erasure transaction (Art. 17)",
	"internal/modules/privacy:deal_room_participant:erasuredealroom.go:anonymizeDealRoomSeats:UPDATE deal_room_participant …":                                  "erasure anonymizes the subject's Deal Room seat in place — the one named outside contact stored without a contact row — in the single erasure transaction",
	"internal/modules/privacy:deal_room_session:erasuredealroom.go:purgeDealRoomSeatTraces:DELETE FROM deal_room_session WHERE participant_id = ANY($1)":       "erasure deletes the erased subject's live room credentials in the same transaction: access they did not consent to keep must not outlive the request",
	"internal/modules/privacy:deal_room_engagement:erasuredealroom.go:purgeDealRoomSeatTraces:DELETE FROM deal_room_engagement WHERE participant_id = ANY($1)": "erasure deletes the erased subject's room activity trail (when they signed in, what they took) in the same transaction",
	"internal/modules/privacy:contact_email:erasure.go:deleteSubjectIdentifierRows:DELETE FROM contact_email WHERE contact_id = $1 OR email = ANY($2)":         "erasure deletes the subject's email channel rows in the single erasure transaction",
	"internal/modules/privacy:contact_email:retentionactions.go:deleteIdentifyingSatellites:DELETE FROM contact_email WHERE contact_id = $1":                   "the contact/anonymize action deletes the subject's address rows in the same transaction, so the record stops naming them by any key it is resolved on — an address left behind re-identifies the row it was stripped from",
	"internal/modules/privacy:preference_token:erasure_consent.go:deleteConsentCapabilities:DELETE FROM preference_token WHERE contact_id = $1":                "erasure deletes the subject's preference-center token in the single erasure transaction — it is a live capability over their consent record on a session-less edge, and anonymize-in-place means 0048's ON DELETE CASCADE never fires, so an erased subject would keep accruing consent rows through the capability the erasure certifies destroyed",
	"internal/modules/privacy:consent_doi_token:erasure_consent.go:deleteConsentCapabilities:DELETE FROM consent_doi_token WHERE contact_id = $1":              "erasure deletes the subject's double-opt-in token in the same transaction and for the same reason as the preference-center token beside it — a bearer secret in their mailbox whose only function is to authorise a grant, which anonymize-in-place would otherwise leave live for its full 72 hours after the erasure certified the data destroyed",
	"internal/modules/privacy:withdrawal_credential:erasure_consent.go:deleteConsentCapabilities:DELETE FROM withdrawal_credential WHERE contact_id = $1":      "erasure destroys the withdrawal link beside the three capabilities around it, and this is the one that would survive longest if missed: it lives 24 months where the preference token lives 30 days, so an overlooked row is a working bearer credential for an erased subject for the better part of two years. It is also the only one of the four carrying the ADDRESS the link was written to, which outlives the contact_id an anonymize-in-place erasure nulls — revoking rather than deleting would leave that address standing in a table nothing else scrubs. Consent owns the table; the delete is here because Art. 17 is privacy's engine to run and it must reach every capability in one transaction, which is the same argument the three writes beside it make",

	"internal/modules/privacy:confirm_token:erasure_consent.go:deleteConsentCapabilities:DELETE FROM confirm_token WHERE contact_id = $1":                               "erasure deletes the confirm-details link beside the two capabilities above, and this is the strongest of the three: the link does not merely authorise a grant, it DISPLAYS the record. Anonymize-in-place leaves the contact row standing, so ON DELETE CASCADE never fires and an erased subject would hold a working view of data the installation certified destroyed",
	"internal/modules/privacy:comms_outbound:erasure_payloads.go:erasePayloads:UPDATE comms_outbound SET payload_ref = NULL …":                                          "erasure clears the vault reference on the subject's controller deliveries, in the same transaction and immediately AFTER destroying the material it names. The order is the reason this write exists here rather than in the column scrub beside it: the reference lives in the row, so nulling it first would orphan a live confirmation link in the vault with no key left to find it, and a failure between the two would roll back with the reference intact so a retry is idempotent",
	"internal/modules/privacy:contact_acquisition_evidence:erasure.go:anonymizeSubjectRows:DELETE FROM contact_acquisition_evidence WHERE contact_id = $1":              "erasure deletes the record of why this contact existed, in the single erasure transaction. It records what the contact did or what was done to obtain them, and has no meaning after them — so it goes with the addresses beside it rather than being tombstoned like a decision, which is the controller's own evidence about a send. A merge MOVES these rows onto the survivor rather than copying them, so the delete by contact_id reaches whichever record the subject now is",
	"internal/modules/privacy:privacy_notice_case:erasure.go:anonymizeSubjectRows:DELETE FROM privacy_notice_case WHERE contact_id = $1":                                "erasure deletes the duty owed for an acquisition it is destroying in the same transaction. The case is a statement ABOUT how this contact was obtained, so once the evidence beside it is gone the case names nothing — and an obligation to tell somebody about data we no longer hold cannot be discharged by anyone. Deleted rather than tombstoned for the same reason the acquisition row is: it has no meaning after the contact",
	"internal/modules/contacts:privacy_notice_case:mergerelink.go:relinkAcquisitionAndDuty:UPDATE privacy_notice_case SET contact_id = $2, updated_at = now() …":        "a merge MOVES the acquisition evidence onto the survivor, and the duty owed for that acquisition is the same fact in a second table — the two move together or the pair stops meaning anything. Left behind it is worse than stale: the case is read off the SURVIVOR's queue, so the workspace still owes a disclosure it can no longer see, and an erasure of the survivor misses a row an erasure of the retired id would have taken. Every satellite in this function moves for that reason and this one is no different",
	"internal/modules/privacy:privacy_notice_case:retentionactions.go:clearCommunicationRecord:DELETE FROM privacy_notice_case WHERE contact_id = $1":                   "the contact/anonymize action clears the same table in the same transaction as the erasure path above, because both acts must clear it: one that cleared the subject's notice cases and one that did not would leave a live Art. 14 deadline pointing at a contact an operator had just been told was anonymized, and the queue would go on showing a duty owed to nobody",
	"internal/modules/privacy:contact_acquisition_evidence:retentionactions.go:clearCommunicationRecord:DELETE FROM contact_acquisition_evidence WHERE contact_id = $1": "the contact/anonymize action deletes the same rows in the same transaction, because both acts must clear the same tables: one that cleared it and one that did not would leave the subject's acquisition record standing after an operator was told the contact had been anonymized",
	"internal/modules/privacy:communication_review:erasure_leadtwins.go:anonymizeLeadTwins:WITH wiped AS ( …":                                                           "the lead-twin sweep empties the refusals a review recorded about the LEAD this contact was promoted from. The contact sweep beside it reaches a review by the contact's own id or one of their addresses, and an unpromoted lead has neither — so a message refused for the lead before promotion would keep their address after the contact it became was erased. Same UPDATE-not-DELETE shape and same reason as the contact half: the work may still be in front of a human, and a row vanishing under them leaves a queue pointing at nothing",
	"internal/modules/privacy:communication_instruction:erasure_consent.go:tombstoneExceptionExplanations:UPDATE communication_instruction …":                           "erasure replaces a director's own sentence about the subject with a tombstone, in the single erasure transaction. The row is otherwise IMMUTABLE — its trigger refuses every other edit — and this is the one door through that freeze, because the explanation is a rep writing about a named contact and an immutable record of it would put the freeze above the Art. 17 right it exists inside. What survives is the accountable half: that somebody overrode a refusal, who they were, when, and under which reason code, none of which names the subject. It UPDATEs rather than deletes for that reason: destroying the row would destroy the evidence that an override happened at all",
	"internal/modules/privacy:communication_review:erasure_consent.go:clearRefusedSendReviews:UPDATE communication_review …":                                            "erasure empties the refusals a review recorded, in the single erasure transaction. It UPDATEs rather than deletes, and the reason is the opposite of the decision beside it: a decision records a message that WAS sent and why it was permitted, which the controller must answer for, while a review records one that was refused and never went — there is nothing to answer for, and what it holds is a snapshot of the subject's addresses. The row survives with its refusals emptied because the work may still be in front of a human, and a row vanishing under them leaves a queue pointing at nothing",

	"internal/modules/privacy:data_subject_request:erasure_consent.go:retireRightsCases:UPDATE data_subject_request …": "both privacy acts take the subject out of their own rights cases through this one writer — the Art. 17 erasure and the contact/anonymize sweep, which a gate requires to clear the same tables. It UPDATEs rather than deletes for the reason the decision row beside it does, and this is the sharpest instance of that reason: the erasure being run IS usually the answer to one of these rows, so destroying the case would leave the controller unable to show it answered the request — including a REJECTED one, where the record is what an appeal or a supervisory authority would ask to see. What goes is the identifying half: subject_ref is the free-text identity somebody typed, and resolution is prose a colleague wrote that can name the subject or quote them. The kind, status, dates and receipt reference stay as an unattributed record that a request of this kind arrived and was answered by its deadline. The resolution is replaced rather than nulled because dsr_resolution_shape requires a closed case to carry one, and a null would abort the whole act on exactly the closed cases it is most likely to find. Consent owns the table; the write is here because Art. 17 and the retention engine are both privacy's to run, and each must reach the subject's whole footprint in one transaction",

	"internal/modules/privacy:communication_decision:erasure_consent.go:deleteConsentCapabilities:UPDATE communication_decision …":                                  "erasure tombstones the recipient address and cuts the subject link on the authorization decisions in the single erasure transaction. It UPDATEs rather than deletes, and that is the whole reason it lives here: the row is the controller's own Art. 5(2) record of why a message was permitted, so destroying it would erase the evidence that sends already made were lawful, while leaving the address would keep the erased mailbox readable on every decision it appears in",
	"internal/modules/privacy:communication_basis:erasure_consent.go:deleteConsentCapabilities:DELETE FROM communication_basis WHERE contact_id = $1":               "erasure deletes the subject's non-consent bases in the same transaction — each row exists only to say something about THIS contact (they wrote to us on a date, in a thread), so unlike the decision beside it a basis has no life after the subject and anonymize-in-place would leave it naming a thread and a contact who asked to be forgotten",
	"internal/modules/privacy:communication_suppression:erasure_consent.go:deleteConsentCapabilities:DELETE FROM communication_suppression …":                       "erasure deletes the subject's objections and restrictions in the same transaction, for the reason the bases go: a suppression is a statement about one contact. It is safe to delete precisely because the contact row is going too — a suppression outliving its subject could only ever match a future record that happened to reuse the address, which is somebody else",
	"internal/modules/privacy:communication_override:erasure_consent.go:deleteConsentCapabilities:DELETE FROM communication_override WHERE contact_id = $1":         "and the subject's standing overrides with them, in the same transaction as the suppressions beside it — a vouch is a statement about THIS contact, and it is safe to delete because the contact row is going too: an override outliving its subject could only ever match a future record that happened to reuse the address, which is somebody else",
	"internal/modules/privacy:communication_override:erasure_leadtwins.go:anonymizeLeadTwins:WITH wiped AS ( …":                                                     "and the lead's standing overrides with them, same statement and same reason as the suppression beside it — a vouch is a statement about this lead and has no address arm to reach, so it is deleted outright here by lead_id",
	"internal/modules/privacy:communication_override:retention_leadrecord.go:clearLeadCommunicationRecord:DELETE FROM communication_override WHERE lead_id = $1":    "an override has no address to carry it forward onto, unlike the suppression above: it is deleted outright, the same verb the Art. 17 lead-twin sweep uses against it",
	"internal/modules/privacy:communication_override:retentionactions.go:clearCommunicationRecord:DELETE FROM communication_override WHERE contact_id = $1":         "an override has no address to detach onto, unlike the suppression above: a subject who returns arrives as a new record with nobody yet vouching for them, so the vouch cannot be carried forward and is deleted outright — the same verb the eraser's contact-keyed delete uses",
	"internal/modules/privacy:contact_confirm_submission:erasure_consent.go:deleteConsentCapabilities:DELETE FROM contact_confirm_submission WHERE contact_id = $1": "the same transaction deletes what came back through that link — the subject's own name and address as they proposed them. Not a capability but subject data, and data the workspace was asked to hold rather than anything it agreed: an unaccepted proposal surviving an erasure is the subject's plaintext outliving the request to destroy it",
	"internal/modules/privacy:withdrawal_credential:retentionactions.go:anonymizeContactRecord:DELETE FROM withdrawal_credential WHERE contact_id = $1":             "the contact/anonymize action deletes the withdrawal link for the reason it deletes the two tokens beside it, plus one this table alone raises: the row carries the ADDRESS the link was written to in its own column, which is exactly the content the anonymization has just cleared from the contact row. Leaving it would keep an anonymized subject's mailbox legible in a table the anonymization did not reach. It is also the longest-lived of the capabilities, at 24 months. Consent owns the table; the delete is here because the retention engine is privacy's to run",

	"internal/modules/privacy:confirm_token:retentionactions.go:anonymizeContactRecord:DELETE FROM confirm_token WHERE contact_id = $1":                           "the contact/anonymize action deletes the confirm link for the reason it deletes the opt-in token beside it, and more urgently: a live link would show an old mailbox the very fields this action has just emptied",
	"internal/modules/privacy:contact_confirm_submission:retentionactions.go:anonymizeContactRecord:DELETE FROM contact_confirm_submission WHERE contact_id = $1": "and the submissions with it — they hold the subject's name and address in plaintext, which is exactly the content the anonymization above just cleared from the contact row",
	"internal/modules/privacy:communication_decision:retentionactions.go:clearCommunicationRecord:UPDATE communication_decision …":                                "the contact/anonymize action tombstones the recipient address and cuts the subject link on the authorization decisions, exactly as the eraser does. Both acts must clear the same tables or a subject anonymized rather than erased keeps their address on every decision naming them, after an operator was told the record was anonymized. The row survives in both because it is the controller's Art. 5(2) evidence that a send already made was lawful",
	"internal/modules/privacy:communication_basis:retentionactions.go:clearCommunicationRecord:DELETE FROM communication_basis WHERE contact_id = $1":             "the same action deletes the subject's non-consent bases, for the reason the eraser deletes them: a basis names a thread and a date and says why writing to THIS contact was lawful. An anonymized subject who returns arrives as a new record, so a surviving basis would authorize writing to them on the strength of a conversation nobody can now identify",
	"internal/modules/privacy:communication_suppression:retentionactions.go:clearCommunicationRecord:DELETE FROM communication_suppression WHERE contact_id = $1": "and the suppressions with them. The suppression LIST is the separate mechanism that keeps an anonymized subject from being re-contacted; a per-contact objection left attached to a record that no longer names anybody protects nobody and would only ever match a future record reusing the address, which is somebody else" +
		" — the DELETE takes what the detach could not reach",
	"internal/modules/privacy:communication_suppression:retentionactions.go:clearCommunicationRecord:UPDATE communication_suppression …": "and the suppressions with them. The suppression LIST is the separate mechanism that keeps an anonymized subject from being re-contacted; a per-contact objection left attached to a record that no longer names anybody protects nobody and would only ever match a future record reusing the address, which is somebody else" +
		" — the UPDATE detaches the suppression onto the bare address, so a stop outlives the record it was recorded against",
	"internal/modules/privacy:consent_doi_token:retentionactions.go:anonymizeContactRecord:DELETE FROM consent_doi_token WHERE contact_id = $1":                                                                                        "the contact/anonymize action deletes the subject's opt-in tokens in the same transaction: an anonymized subject may lawfully return, but they return by being invited again, not by an old token in an old mailbox still working",
	"internal/modules/privacy:activity_identity:activitycontenterasure.go:Eraser.purgeContentDerivedFrom:DELETE FROM activity_identity WHERE activity_id = $1":                                                                         "erasure ARCHIVES an activity rather than deleting it, so the identity table's foreign-key cascade never fires and the external identities the message answered to would outlive the content they point at: the next arrival of that message resolves onto an emptied row, binds to it, and in doing so discloses that the message was erased. It is destroyed in the same transaction as the text it identifies, beside the other derived rows this function purges, because an identity surviving its content by one failed follow-up call is the exact shape purgeContentDerivedFrom exists to prevent",
	"internal/modules/privacy:activity_participant:erasure_graph.go:subjectParticipantsBlank:UPDATE activity_participant ap …":                                                                                                         "erasure nulls the subject's contact, address and channel-account arms on the interaction participants in the single erasure transaction — the address and account arms exist precisely for a party who never became a record (a chat roster names the third human in a group by account alone), so they survive the contact_email and contact_channel_identity purges and would keep the erased subject readable and re-matchable; the ROW is kept where other participants remain, because the other contacts in that conversation are not the subject",
	"internal/modules/privacy:activity_participant:erasure_graph.go:subjectParticipantsDelete:DELETE FROM activity_participant ap …":                                                                                                   "erasure nulls the subject's contact, address and channel-account arms on the interaction participants in the single erasure transaction — the address and account arms exist precisely for a party who never became a record (a chat roster names the third human in a group by account alone), so they survive the contact_email and contact_channel_identity purges and would keep the erased subject readable and re-matchable; the ROW is kept where other participants remain, because the other contacts in that conversation are not the subject",
	"internal/modules/privacy:activity_participant:retention_graph.go:sweptParticipantsDelete:DELETE FROM activity_participant ap …":                                                                                                   "the nightly retention sweep nulls the subject's contact, address and channel-account arms on the interaction participants, inside the retention engine's per-record transaction — the address and account arms exist for a party who never became a record (a chat roster names the third human in a group by account alone), so they survive the contact_email and contact_channel_identity purges and would keep the swept subject readable and re-matchable; the ROW is kept where other participants remain, because the other contacts in that conversation are not the subject",
	"internal/modules/privacy:activity_participant:retention_graph.go:sweptParticipantsBlank:UPDATE activity_participant ap …":                                                                                                         "the nightly retention sweep nulls the subject's contact, address and channel-account arms on the interaction participants, inside the retention engine's per-record transaction — the address and account arms exist for a party who never became a record (a chat roster names the third human in a group by account alone), so they survive the contact_email and contact_channel_identity purges and would keep the swept subject readable and re-matchable; the ROW is kept where other participants remain, because the other contacts in that conversation are not the subject",
	"internal/modules/identity:linkedin_connection:users.go:Service.revokeBorrowedAuthority:DELETE FROM linkedin_connection WHERE owner_user_id = $1":                                                                                  "deactivation deletes the departing user's imported LinkedIn network in the single deactivation transaction — it is their private address book of third parties whose only tie to this installation was that contact's employment, so it cannot outlive the employment; doing it here keeps it atomic with the session and passport revocation rather than leaving a window in which the account is gone and the address book is not",
	"internal/modules/privacy:weekly_plan_commitment:erasure_commitments.go:redactCommitmentsNaming:UPDATE weekly_plan_commitment …":                                                                                                   "erasure redacts what a colleague wrote about the subject on their own weekly plan, in the single erasure transaction. The link accepts 'contact', so a rep can commit to an action about a contact and type a free-text label, help request and manager answer naming them — and no other clause in the cascade can see those rows: the table carries no contact FK for a schema cascade to walk and is keyed to the app_user who wrote it rather than to the subject. Redacted rather than deleted, because the ROW is the rep's work record and the erasure request is the contact's: it reaches the contact's data and stops at the employee's record of having worked",
	"internal/modules/privacy:linkedin_connection:erasure_graph.go:deleteSubjectLinkedInGhosts:DELETE FROM linkedin_connection g …":                                                                                                    "erasure deletes the subject's LinkedIn ghosts in the single erasure transaction — a ghost holds the subject's name, employer and address, imported from a colleague's export without the subject ever being asked, and it is invisible to every contact-keyed clause because a ghost is not a contact row",
	"internal/modules/privacy:graph_interaction_edge:erasure_graph.go:deleteSubjectInteractionEdges:DELETE FROM graph_interaction_edge WHERE contact_id = $1":                                                                          "erasure drops the subject's interaction edges in the single erasure transaction rather than leaving it to the cg:graph-edge consumer — an Art. 17 obligation discharged by an event is one that fails silently when the bus is behind, and the projection holds who corresponded with the subject, how often and how recently",
	"internal/modules/privacy:graph_interaction_edge:retention_graph.go:scrubContactGraphTraces:DELETE FROM graph_interaction_edge WHERE contact_id = $1":                                                                              "the nightly retention sweep drops the subject's interaction edges inside the retention engine's per-record transaction — an edge names who spoke to whom, so it re-identifies the swept party from the graph alone",
	"internal/modules/privacy:graph_contact_edge:erasure_graph.go:deleteSubjectInteractionEdges:DELETE FROM graph_contact_edge WHERE contact_a = $1 OR contact_b = $1":                                                                 "erasure drops the subject's contact↔contact edges, both endpoint columns, in the single erasure transaction — the same Art. 17 reasoning as the interaction projection above, and the far-end column is the copy-paste that would otherwise be missed",
	"internal/modules/privacy:graph_contact_edge:retention_graph.go:scrubContactGraphTraces:DELETE FROM graph_contact_edge WHERE contact_a = $1 OR contact_b = $1":                                                                     "the nightly retention sweep drops the swept party's contact↔contact edges on either end inside the per-record transaction — the pair re-identifies them from the graph alone whichever side they stand on",
	"internal/modules/privacy:capture_pending_counterparty:erasure.go:deleteSubjectIdentifierRows:DELETE FROM capture_pending_counterparty …":                                                                                          "erasure drops the capture dispositions keyed on the subject's own address in the single erasure transaction — left behind, they would keep the erased address readable and still answering the capture gates",
	"internal/modules/privacy:capture_pending_counterparty:retentionactions.go:anonymizeContactRecord:DELETE FROM capture_pending_counterparty WHERE email = ANY($1)":                                                                  "the contact/anonymize action drops the capture discovery rows in the same transaction — a pending counterparty is the subject's address waiting to become a record, so leaving it re-mints them on the next ingest",
	"internal/modules/privacy:contact_social:erasure.go:deleteSubjectIdentifierRows:DELETE FROM contact_social WHERE contact_id = $1":                                                                                                  "erasure and retention delete the subject's social-handle rows in the same anonymization transaction",
	"internal/modules/privacy:contact_social:retentionactions.go:deleteIdentifyingSatellites:DELETE FROM contact_social WHERE contact_id = $1":                                                                                         "erasure and retention delete the subject's social-handle rows in the same anonymization transaction",
	"internal/modules/privacy:voice_learning_signal:retentionai.go:RetentionService.eraseVoiceSignalContent:UPDATE voice_learning_signal …":                                                                                            "the nightly retention sweep erases over-age draft plaintext in place; the counters row survives (voice_draftread.go stamps the per-row deadline)",
	"internal/modules/privacy:contact_phone:erasure.go:deleteSubjectIdentifierRows:DELETE FROM contact_phone WHERE contact_id = $1":                                                                                                    "erasure deletes the subject's phone channel rows in the single erasure transaction",
	"internal/modules/privacy:contact_phone:retentionactions.go:deleteIdentifyingSatellites:DELETE FROM contact_phone WHERE contact_id = $1":                                                                                           "the contact/anonymize action deletes the subject's phone rows in the same transaction, so the record stops naming them by any key it is resolved on",
	"internal/modules/privacy:contact_channel_identity:erasure_channels.go:eraseChannelIdentities:DELETE FROM contact_channel_identity WHERE provider = $1 AND channel_user_id = $2":                                                   "erasure and the retention anonymizer delete the subject's channel-identity rows in the single erasure/per-record transaction — the identity is the key an inbound message would re-bind them by, so it has to go in the same commit that hashes it onto the suppression list",
	"internal/modules/privacy:contact_channel_identity:retentionactions.go:deleteIdentifyingSatellites:DELETE FROM contact_channel_identity WHERE contact_id = $1":                                                                     "erasure and the retention anonymizer delete the subject's channel-identity rows in the single erasure/per-record transaction — the identity is the key an inbound message would re-bind them by, so it has to go in the same commit that hashes it onto the suppression list",
	"internal/modules/privacy:lead:erasure_leadtwins.go:anonymizeLeadTwins:WITH wiped AS ( …":                                                                                                                                          "erasure/retention anonymize the subject's segregated lead rows in the same transaction",
	"internal/modules/privacy:lead:retention_leadrecord.go:RetentionService.anonymizeLead:UPDATE lead SET full_name = 'Anonymized Lead', email = NULL, title = NULL, …":                                                                "erasure/retention anonymize the subject's segregated lead rows in the same transaction",
	"internal/modules/privacy:lead_score_history:erasure_leadtwins.go:anonymizeLeadTwins:WITH wiped AS ( …":                                                                                                                            "the score's explanation is about the subject: its factors embed the ids of activities they took part in, inside JSON no field-level scrub reaches, and the lead is ANONYMIZED rather than deleted so nothing cascades",
	"internal/modules/privacy:lead_score_history:retention_leadrecord.go:RetentionService.anonymizeLead:DELETE FROM lead_score_history WHERE lead_id = $1":                                                                             "the score's explanation is about the subject: its factors embed the ids of activities they took part in, inside JSON no field-level scrub reaches, and the lead is ANONYMIZED rather than deleted so nothing cascades",
	"internal/modules/privacy:lead_manual_signal:erasure_leadtwins.go:anonymizeLeadTwins:WITH wiped AS ( …":                                                                                                                            "a manual scoring signal is a colleague's written judgement about the subject, carrying their name — it cannot outlive the record it judges, and the anonymize fires no cascade",
	"internal/modules/privacy:lead_manual_signal:retention_leadrecord.go:RetentionService.anonymizeLead:DELETE FROM lead_manual_signal WHERE lead_id = $1":                                                                             "a manual scoring signal is a colleague's written judgement about the subject, carrying their name — it cannot outlive the record it judges, and the anonymize fires no cascade",
	"internal/modules/privacy:activity:erasure_restrict.go:restrictShieldedTimeline:UPDATE activity a SET …":                                                                                                                           "a restriction shields a timeline row instead of destroying it (Art. 18): the content stays, readable by nobody, until the hold lifts — so the restriction must be stamped in the same transaction that decided it, or a row is briefly readable after the decision that shielded it",
	"internal/modules/privacy:activity:erasure_restrict.go:stampLegacyHandelsbriefe:), stamped AS ( …":                                                                                                                                 "commercial correspondence carries a statutory retention floor that outranks an erasure request, and the floor is stamped onto the row in the erasure's own transaction — an unstamped Handelsbrief is one the next sweep destroys",
	"internal/modules/privacy:activity:erasuretimeline.go:redactSubjectTimeline:UPDATE activity a SET subject = $5, body = NULL, raw = NULL, …":                                                                                        "Art. 17 redaction nulls the subject's timeline content in the single erasure transaction — a timeline row is other contacts's record too, so it is redacted in place rather than deleted",
	"internal/modules/privacy:activity:restrictionoverride.go:Eraser.PinToFloor:UPDATE activity a …":                                                                                                                                   "a controller pinning a record to its retention floor writes the hold onto the activity in the deciding transaction — the pin IS the decision, and a gap between them is a window in which the sweep can still take the row",
	"internal/modules/privacy:activity:retentionactions.go:RetentionService.archiveActivity:UPDATE activity SET archived_at = now() WHERE id = $1":                                                                                     "the time-based sweep archives an activity that has outlived its class, inside the retention engine's per-record transaction",
	"internal/modules/privacy:activity:retentionactions.go:RetentionService.eraseActivityContent:UPDATE activity SET body = NULL, raw = NULL, subject = $2, language = NULL, archived_at = coalesce(archived_at, now()) WHERE id = $1": "the time-based sweep nulls an expired activity's content in the retention engine's per-record transaction, leaving the timeline row other contacts still need",
	"internal/modules/privacy:activity:retentionrestricted.go:liftAndEraseHeldRecord:UPDATE activity a …":                                                                                                                              "a restriction expiring is the one moment a held row may be destroyed, and the lift and the erasure are ONE statement because the data-layer guard admits nothing else — a lift that left the body readable would undo the restriction and keep the data",
	"internal/modules/privacy:activity_retention_evidence:erasure_restrict.go:stampLegacyHandelsbriefe:), stamped AS ( …":                                                                                                              "Art. 17 erasure stamps a Handelsbrief captured before the stamp writer existed, and writes the evidence its restriction rests on, in the single erasure transaction — the guard refuses a restriction with no evidence behind it, and for a pre-stamp row the deal links the erasure reads are the only evidence there is",
	"internal/modules/privacy:activity_retention_evidence:restrictionoverride.go:recordPinEvidence:INSERT INTO activity_retention_evidence …":                                                                                          "Art. 17 erasure stamps a Handelsbrief captured before the stamp writer existed, and writes the evidence its restriction rests on, in the single erasure transaction — the guard refuses a restriction with no evidence behind it, and for a pre-stamp row the deal links the erasure reads are the only evidence there is",
	"internal/modules/privacy:attachment:erasure_attachments.go:Eraser.eraseAttachments:DELETE FROM attachment WHERE":                                                                                                                  "Art. 17 erasure deletes attachments hung off the subject or a subject-only activity in the single erasure transaction",
	"internal/modules/privacy:deal:retentionactions.go:RetentionService.archiveDeal:UPDATE deal SET archived_at = now() WHERE id = $1":                                                                                                 "retention archives over-age lost deals per its audited per-record transaction",
	"internal/modules/privacy:activity_reply_verdict_history:activitycontenterasure.go:Eraser.purgeContentDerivedFrom:DELETE FROM activity_reply_verdict_history WHERE activity_id = $1":                                               "the same rows for ONE activity rather than for a subject. This arm runs when a single record's content is destroyed on its own — a retention window closing, a statutory floor expiring, a controller releasing a restriction — and no contact is being erased, so the subject-scoped helper below never fires for it. A verdict surviving here would outlive the very message it was read from, which is the invariant this file exists to hold for every other derived artifact beside it",
	"internal/modules/privacy:sdr_handoff:erasuretimeline.go:deleteSubjectHandoffs:DELETE FROM sdr_handoff …":                                                                                                                          "erasure and anonymization destroy the handoffs naming the subject. A handoff carries their id, a free-text note one seat wrote ABOUT them, and a judgement colleagues made about whether they were worth working — the holding ai_feedback is registered for, deleted rather than nulled on the same ground. The foreign keys cascade on DELETE and both acts UPDATE the contact in place, so the cascade never fires for an Art. 17 request and this statement is what reaches them. It lives in privacy because the subject scoping is the erasure's question, not the handoff's",
	"internal/modules/privacy:sdr_handoff_event:erasuretimeline.go:deleteSubjectHandoffs:DELETE FROM sdr_handoff_event …":                                                                                                              "the transitions behind those handoffs, deleted by name in the same function. They would go through the parent's own cascade anyway — this statement exists so the census that asks whether Art. 17 reaches a table can SEE that it does, and so the next reader auditing what this erasure destroys does not have to infer it from a foreign key",
	"internal/modules/privacy:activity_reply_verdict_history:erasuretimeline.go:deleteReplyVerdictHistoryFor:DELETE FROM activity_reply_verdict_history …":                                                                             "erasure and anonymization destroy what a classifier concluded a subject's messages MEANT, along with every human correction of it. The rows are derived from activity text, so a verdict left standing would keep the claim that somebody replied negatively after the words it was read from are gone — and under an anonymize, where the text SURVIVES, it would go on being read as a live conclusion about a contact the record no longer names. ONE function, called by both acts, which is what the anonymize-parity gate requires: a history cleared by only one of them holds the subject's data after an operator was told it was gone. It lives in privacy rather than activities because the subject scoping is the erasure's question, not the timeline's",
	"internal/modules/privacy:activity_request_settlement:erasuretimeline.go:deleteRequestSettlementsFor:DELETE FROM activity_request_settlement …":                                                                                    "erasure and anonymization destroy what this installation concluded our own replies DID about what the subject asked — a verdict, and where the reply left something undone, a sentence the model wrote naming what we still owe them. The same ground the reply verdicts above stand on: the rows are read out of activity text, so a settlement left standing would keep our reading after the words are gone, and under an anonymize — where the text survives — it would go on naming an outstanding obligation to a record that no longer names anybody. ONE function, called by both acts, which is what the anonymize-parity gate requires. It reaches the rows by BOTH the link walk and the unlinked-mail selector, because a request carrying no contact link is the ordinary shape since ADR-0072 and the settlement pass judges one happily. It lives in privacy rather than activities because the subject scoping is the erasure's question, not the timeline's",
	"internal/modules/privacy:embedding:erasure.go:purgeContactDerivedRows:DELETE FROM embedding WHERE entity_type = 'contact' AND entity_id = $1":                                                                                     "erasure/retention purge the subject's vectors — a similarity probe must not reconstruct erased text",
	"internal/modules/privacy:embedding:erasure.go:purgeRedactedActivityTraces:DELETE FROM embedding WHERE entity_type = 'activity' AND entity_id = ANY($1)":                                                                           "erasure/retention purge the subject's vectors — a similarity probe must not reconstruct erased text",
	"internal/modules/privacy:embedding:erasure_restrict.go:holdShieldedTimeline:DELETE FROM embedding WHERE entity_type = 'activity' AND entity_id = ANY($1)":                                                                         "erasure/retention purge the subject's vectors — a similarity probe must not reconstruct erased text",
	"internal/modules/privacy:embedding:erasuretimeline.go:subjectActivityEmbeddingsDelete:DELETE FROM embedding e USING activity_link l …":                                                                                            "erasure/retention purge the subject's vectors — a similarity probe must not reconstruct erased text",
	"internal/modules/privacy:embedding:restrictionoverride.go:pinnedRecordLeavesDerivedCopies:DELETE FROM embedding WHERE entity_type = 'activity' AND entity_id = $1":                                                                "erasure/retention purge the subject's vectors — a similarity probe must not reconstruct erased text",
	"internal/modules/privacy:embedding:retention_leadrecord.go:RetentionService.anonymizeLead:DELETE FROM embedding WHERE entity_type = 'lead' AND entity_id = $1":                                                                    "erasure/retention purge the subject's vectors — a similarity probe must not reconstruct erased text",
	"internal/modules/privacy:embedding:retentionactions.go:anonymizeContactRecord:DELETE FROM embedding WHERE entity_type = 'contact' AND entity_id = $1":                                                                             "erasure/retention purge the subject's vectors — a similarity probe must not reconstruct erased text",
	"internal/modules/privacy:embedding:activitycontenterasure.go:Eraser.purgeContentDerivedFrom:DELETE FROM embedding WHERE entity_type = 'activity' AND entity_id = $1":                                                              "erasure/retention purge the subject's vectors — a similarity probe must not reconstruct erased text",
	"internal/modules/activities:embedding:capturenoise.go:Store.RedactCapturedNoiseTx:DELETE FROM embedding WHERE entity_type = 'activity' AND entity_id = ANY($1)":                                                                   "the ADR-0072 noise redaction drops the vectors built from the mail it just nulled, in the same transaction — an embedding of redacted text is that text in another shape, and leaving it would let a similarity probe reconstruct what the workspace decided not to retain. Same obligation as the privacy waiver above, at the other place content is destroyed; the embed lane cannot do it itself because it never observes an archived row",
	"internal/modules/activities:embedding:audienceretraction.go:RetractDerivedForActivityTx:DELETE FROM embedding WHERE entity_type = 'activity' AND entity_id = $1":                                                                  "narrowing a message's audience drops the vector built from it, in the same transaction as the narrowing. The obligation is the sibling of the noise redaction above and the reason is the same shape: an embedding is the message's text in another shape, it is built as the system principal over the whole workspace and queried by everyone, so leaving it lets a similarity probe reach a message the narrowing just withheld. The embed lane cannot do it itself — its query returns no row for a narrowed message, and no row means 'nothing to index', never 'delete what is there'",
	"internal/modules/privacy:capture_trace:erasuretimeline.go:purgeDerivedTraces:DELETE FROM capture_trace …":                                                                                                                         "erasure deletes the subject's rows from the 24-hour capture trace in the single erasure transaction, beside raw_capture and ai_call_payload above. The trace's sweep bounds exposure to a day; it does not ANSWER a request made inside that day, and an erasure honoured everywhere except one diagnostic table is not honoured. Only the payload columns can name anybody, and on the default posture they DO: capture.trace_payloads ships on, because a trace naming nobody cannot say why a message went missing. So this statement is the one that carries the erasure for a table that normally holds identifiers, rather than a precaution against a posture few installations choose. Exact equality rather than the ILIKE its neighbours use, because this column is written normalized and indexed",
	"internal/modules/privacy:capture_trace:erasure_channels.go:purgeChannelCaptureTrace:DELETE FROM capture_trace …":                                                                                                                  "the OTHER half of the same erasure, and a separate waiver because the key names the function. The lane above matches an ADDRESS; a counterparty the pipeline knew by a provider account is named in the trace by their display name instead (capture/trace.go, traceChannelPayload), so a subject who only ever wrote from a channel had every trace row survive an erasure that reported success. Scoped to the providers the subject is actually bound on, so it cannot reach a same-named contact on a transport they never used. It lives in privacy rather than capture for the same reason its sibling does: the erasure is one transaction, and a cascade that left one table to another module's scheduler would be a cascade with a gap in it",
	"internal/modules/privacy:raw_capture:erasure_channels.go:purgeChannelRawCapture:DELETE FROM raw_capture …":                                                                                                                        "erasure purges raw provider payloads carrying the subject's identifiers in the single erasure transaction",
	"internal/modules/privacy:raw_capture:erasuretimeline.go:purgeDerivedTraces:DELETE FROM raw_capture WHERE payload::text ILIKE '%' || $1 || '%' ESCAPE '\\'":                                                                        "erasure purges raw provider payloads carrying the subject's identifiers in the single erasure transaction",
	"internal/modules/privacy:contact_profile_field:erasure.go:purgeContactDerivedRows:DELETE FROM contact_profile_field WHERE contact_id = $1":                                                                                        "Art. 17 and the retention sweep delete the subject's enrichment sidecar inside the single erasure transaction, beside field_provenance and ai_feedback: anonymize-in-place leaves the contact row standing, so nothing cascades here, and the row holds the subject's title and employer with the verbatim sentence naming them",
	"internal/modules/privacy:contact_profile_field:retentionactions.go:deleteIdentifyingSatellites:DELETE FROM contact_profile_field WHERE contact_id = $1":                                                                           "Art. 17 and the retention sweep delete the subject's enrichment sidecar inside the single erasure transaction, beside field_provenance and ai_feedback: anonymize-in-place leaves the contact row standing, so nothing cascades here, and the row holds the subject's title and employer with the verbatim sentence naming them",
	"internal/modules/privacy:contact_provider_claim:erasure_provider.go:purgeProviderPurchases:DELETE FROM contact_provider_claim WHERE contact_id = ANY($1)":                                                                         "Art. 17 and the retention sweep delete what a licensed data provider asserted about the subject, inside the single erasure transaction and for the same reason contact_profile_field is here: anonymize-in-place leaves the contact row standing, so nothing cascades, and a claim IS the purchased value — a bought email and employer would otherwise sit on the page beside an \"Erased Subject\" name. Deleted rather than nulled, because a claim nulled in place is a row asserting something about a contact nobody may now assert anything about",
	"internal/modules/privacy:contact_provider_claim:retentionactions.go:purgeSubjectPurchases:DELETE FROM contact_provider_claim WHERE contact_id = $1":                                                                               "Art. 17 and the retention sweep delete what a licensed data provider asserted about the subject, inside the single erasure transaction and for the same reason contact_profile_field is here: anonymize-in-place leaves the contact row standing, so nothing cascades, and a claim IS the purchased value — a bought email and employer would otherwise sit on the page beside an \"Erased Subject\" name. Deleted rather than nulled, because a claim nulled in place is a row asserting something about a contact nobody may now assert anything about",
	"internal/modules/privacy:provider_applied_field:erasure_provider.go:purgeProviderPurchases:DELETE FROM provider_applied_field WHERE contact_id = ANY($1)":                                                                         "the same erasure, one table over: what a purchase FILLED on the record, deleted beside the claims that carried it. The row keeps the value it wrote for a plain column — the title, the profile URL — because the revert has nothing else to recognise its own value by, so it is subject data and cannot outlive the subject. Anonymize-in-place leaves the contact row standing, so the foreign key's cascade never fires and this statement is what removes them",
	"internal/modules/privacy:provider_applied_field:retentionactions.go:purgeSubjectPurchases:DELETE FROM provider_applied_field WHERE contact_id = $1":                                                                               "the retention sweep's half of the same delete, for the same reason: a bought title left in this table would name an \"Erased Subject\" and still say what their job was",
	"internal/modules/privacy:provider_run:erasure_provider.go:purgeProviderPurchases:UPDATE provider_run SET":                                                                                                                         "the same two paths scrub the runs that bought that data, using storekit.ScrubProviderRunColumns — the SAME six-column clause integrations' own delete-data action uses, shared precisely because the two drifted once and the erasure cleaned less than the settings toggle did. It is a scrub and not a delete on purpose: the row stops naming anybody while the spend it records survives, because what the installation paid is an accounting fact about the installation once it names no one (PI-AC-8), and that is what keeps a spend history stable across an erasure",
	"internal/modules/privacy:provider_run:retentionactions.go:purgeSubjectPurchases:UPDATE provider_run SET":                                                                                                                          "the same two paths scrub the runs that bought that data, using storekit.ScrubProviderRunColumns — the SAME six-column clause integrations' own delete-data action uses, shared precisely because the two drifted once and the erasure cleaned less than the settings toggle did. It is a scrub and not a delete on purpose: the row stops naming anybody while the spend it records survives, because what the installation paid is an accounting fact about the installation once it names no one (PI-AC-8), and that is what keeps a spend history stable across an erasure",
	"internal/modules/contacts:provider_run:mergerelink.go:relinkProviderPurchases:UPDATE provider_run SET contact_id = $2 WHERE contact_id = $1": "the contact merge relinks the merged-away record's runs onto the survivor, and must decide the collision between two live runs where the unique index admits only one. It happens inside the merge transaction because that is the only place both contact ids and both sides' run states are known at once; integrations cannot see a merge and contacts cannot hand the decision over mid-transaction. The write touches contact_id, state and input_fingerprint only — never a reservation, never a credit — and the rule it enforces is integrations' own: a run past `queued` may have been paid for, so it is re-fingerprinted out of the live-run index rather than cancelled, the same idiom markSkipped uses" +
		" — the repoint onto the survivor",
	"internal/modules/contacts:provider_run:mergerelink.go:relinkProviderPurchases:UPDATE provider_run s …": "the contact merge relinks the merged-away record's runs onto the survivor, and must decide the collision between two live runs where the unique index admits only one. It happens inside the merge transaction because that is the only place both contact ids and both sides' run states are known at once; integrations cannot see a merge and contacts cannot hand the decision over mid-transaction. The write touches contact_id, state and input_fingerprint only — never a reservation, never a credit — and the rule it enforces is integrations' own: a run past `queued` may have been paid for, so it is re-fingerprinted out of the live-run index rather than cancelled, the same idiom markSkipped uses" +
		" — the fingerprint rewrites that keep a cancelled or in-flight run from colliding with the survivor's own",
	"internal/modules/contacts:provider_run:mergerelink.go:relinkProviderPurchases:UPDATE provider_run …": "the contact merge relinks the merged-away record's runs onto the survivor, and must decide the collision between two live runs where the unique index admits only one. It happens inside the merge transaction because that is the only place both contact ids and both sides' run states are known at once; integrations cannot see a merge and contacts cannot hand the decision over mid-transaction. The write touches contact_id, state and input_fingerprint only — never a reservation, never a credit — and the rule it enforces is integrations' own: a run past `queued` may have been paid for, so it is re-fingerprinted out of the live-run index rather than cancelled, the same idiom markSkipped uses" +
		" — cancelling the merged-away record's queued runs before the repoint, so a run cannot complete against a record that no longer exists",
	"internal/modules/privacy:ai_feedback:erasure_leadtwins.go:anonymizeLeadTwins:WITH wiped AS ( …":                                                                          "Art. 17 deletes the subject's correction ledger inside the single erasure transaction, exactly as it does field_provenance beside it: the ledger holds a human-typed value ABOUT the subject, and a claim nobody may now assert anything about has nothing left to suppress",
	"internal/modules/privacy:ai_feedback:erasure.go:purgeContactDerivedRows:DELETE FROM ai_feedback WHERE subject_type = 'contact' AND subject_id = $1":                      "Art. 17 deletes the subject's correction ledger inside the single erasure transaction, exactly as it does field_provenance beside it: the ledger holds a human-typed value ABOUT the subject, and a claim nobody may now assert anything about has nothing left to suppress",
	"internal/modules/privacy:ai_feedback:retentionactions.go:anonymizeContactRecord:DELETE FROM ai_feedback WHERE subject_type = 'contact' AND subject_id = $1":              "the contact/anonymize action deletes the verdicts recorded about the subject in the same transaction — a verdict carries the corrected VALUE a human typed, which is the subject's own data in a table nothing else on this path reaches",
	"internal/modules/privacy:ai_call_payload:erasuretimeline.go:purgeDerivedTraces:DELETE FROM ai_call_payload p …":                                                          "erasure purges captured AI payloads whose CALL cited a record this erasure destroyed — the subject, an activity of theirs, a lead wiped with them — in the same transaction as the content match beside it. It is the lane that reaches a meeting transcript, which names its speakers rather than addressing them and may spell no address at all; the ai_call metadata row survives either way",
	"internal/modules/privacy:ai_call_payload:erasuretimeline.go:purgeDerivedTraces:DELETE FROM ai_call_payload …":                                                            "erasure purges captured AI payloads mentioning the subject's identifiers, and retention ages every payload out at 365d — the special-category-adjacent content, deleted in the single erasure/per-record transaction while the ai_call metadata row survives",
	"internal/modules/privacy:ai_call_payload:retentionrowerasure.go:RetentionService.erasePayload:DELETE FROM ai_call_payload WHERE id = $1":                                 "erasure purges captured AI payloads mentioning the subject's identifiers, and retention ages every payload out at 365d — the special-category-adjacent content, deleted in the single erasure/per-record transaction while the ai_call metadata row survives",
	"internal/modules/privacy:raw_capture:retentionrowerasure.go:RetentionService.eraseRawCapture:DELETE FROM raw_capture WHERE id = $1":                                      "the retention engine ages a stored provider original on its own clock, and deletes it BY ID from the ids its own selector produced — none of the (source_system, source_id) join knowledge PurgeRawCaptureTx exists to keep inside capture, which is why this is not that seam being bypassed; routing a primary-key delete through capture would mean the owning module growing an entry point only the engine calls, and the pass's audited per-record transaction is where the delete has to happen",
	"internal/modules/privacy:ai_call:retentionai.go:RetentionService.eraseEmbedCall:DELETE FROM ai_call WHERE id = $1":                                                       "retention erases embedding-kind ai_call trace rows past their fixed 90-day cap (spec §4) in the single erasure/per-record transaction — a fixed operational cap, not an admin-editable retention_policy row",
	"internal/modules/consent:retention_policy:retention.go:SeedDefaultRetentionTx:INSERT INTO retention_policy (object_type, category, retain_days, action, lawful_basis) …": "bootstrap plants the DM-SEED-1..6 defaults inside the workspace-creation transaction, beside the consent purposes it ships with, so a new installation is compliant before it serves a request. Boot-time only and one row per scope — the table's store, its RBAC gate and every runtime write live in privacy, which owns it",
	"internal/modules/privacy:communication_decision:erasure_leadtwins.go:anonymizeLeadTwins:WITH wiped AS ( …":                                                               "the lead arm of the same tombstone the contact arm applies in erasure_consent.go. A decision written while the subject was still a lead carries subject_kind='lead', which the contact-keyed statement cannot see — and this is an ANONYMIZE, so the lead row survives and no cascade fires. Left alone the row keeps a live recipient address and a lead id that still points at the erased contact",
	"internal/modules/privacy:communication_basis:retention_leadrecord.go:clearLeadCommunicationRecord:DELETE FROM communication_basis WHERE lead_id = $1":                    "retention anonymizes the lead in place, so ON DELETE CASCADE never fires and a basis naming the lead would outlive the scrub, still carrying the thread it was earned on and the message that earned it",
	"internal/modules/privacy:communication_suppression:retention_leadrecord.go:clearLeadCommunicationRecord:DELETE FROM communication_suppression WHERE lead_id = $1": "the objection is DETACHED onto the address rather than deleted, because an anonymized subject may lawfully return and a deleted objection means they return mailable; the delete that follows reaches only rows the detach could not, whose lead is going" +
		" — the DELETE takes what the detach could not reach",
	"internal/modules/privacy:communication_suppression:retention_leadrecord.go:clearLeadCommunicationRecord:UPDATE communication_suppression …": "the objection is DETACHED onto the address rather than deleted, because an anonymized subject may lawfully return and a deleted objection means they return mailable; the delete that follows reaches only rows the detach could not, whose lead is going" +
		" — the UPDATE detaches the suppression onto the bare address, so a stop outlives the record it was recorded against",
	"internal/modules/privacy:communication_decision:retention_leadrecord.go:clearLeadCommunicationRecord:UPDATE communication_decision …": "a decision written about the lead carries their address and their id, which the anonymize does not reach: the lead row survives the scrub, so nothing cascades and the row would keep a live recipient address",
	"internal/modules/privacy:communication_basis:erasure_leadtwins.go:anonymizeLeadTwins:WITH wiped AS ( …":                               "the lead arm of the basis delete beside it, in the same statement as the four per-lead scrubs above, and for the reason they are there: the lead row is anonymized rather than deleted, so ON DELETE CASCADE never fires and a basis naming the lead would outlive the erasure",
	"internal/modules/privacy:withdrawal_credential:erasure_leadtwins.go:anonymizeLeadTwins:WITH wiped AS ( …":                             "the lead-twin sweep destroys withdrawal links the contact-keyed delete cannot see, and there are two such rows. A credential minted while the recipient existed only as a lead carries lead_id and no contact_id, so promoting that lead and then erasing the contact left it standing. A credential minted for an address no record held carries NEITHER id, so nothing keyed by subject ever reaches it. Both hold the address in plaintext, which is the content this sweep exists to remove, and both remain WORKING capabilities for 24 months. It is keyed here on the wiped lead ids and on the subject's own addresses, which is the same pair the communication_suppression delete beside it uses. Consent owns the table; the delete is here because Art. 17 is privacy's engine to run and it must reach every capability in one transaction",

	"internal/modules/privacy:communication_suppression:erasure_leadtwins.go:anonymizeLeadTwins:WITH wiped AS ( …":                                                                      "and the lead's suppressions with them, same statement and same reason — safe to delete here because the eraser hashes every address onto erasure_suppression, so the subject cannot be re-captured at all",
	"internal/modules/privacy:field_provenance:erasure_leadtwins.go:anonymizeLeadTwins:WITH wiped AS ( …":                                                                               "Art. 17 erasure deletes the subject's field-origin metadata in the single erasure transaction — provenance must not outlive the fields it annotates",
	"internal/modules/privacy:field_provenance:erasure.go:purgeContactDerivedRows:DELETE FROM field_provenance WHERE object_type = 'contact' AND object_id = $1":                        "Art. 17 erasure deletes the subject's field-origin metadata in the single erasure transaction — provenance must not outlive the fields it annotates",
	"internal/modules/privacy:field_provenance:erasuretimeline.go:redactSubjectTimeline:DELETE FROM field_provenance …":                                                                 "Art. 17 erasure deletes the subject's field-origin metadata in the single erasure transaction — provenance must not outlive the fields it annotates",
	"internal/modules/privacy:field_provenance:retentionactions.go:anonymizeContactRecord:DELETE FROM field_provenance WHERE object_type = 'contact' AND object_id = $1":                "the contact/anonymize action deletes the field-level provenance in the same transaction — provenance records what each stripped field used to hold and who put it there, so it outlives the value it describes unless it goes with it",
	"internal/modules/privacy:field_provenance:activitycontenterasure.go:Eraser.purgeContentDerivedFrom:DELETE FROM field_provenance WHERE object_type = 'activity' AND object_id = $1": "Art. 17 erasure deletes the subject's field-origin metadata in the single erasure transaction — provenance must not outlive the fields it annotates",
	"internal/modules/privacy:communication_review:scheduledsends.go:closeReviewsForErasedMessages:UPDATE communication_review …":                                                       "erasure ENDS the reviews standing over the messages it just cancelled, in the same transaction. clearRefusedSendReviews beside it empties a review's refusals and deliberately leaves the row LIVE so a human's queue does not develop holes — which leaves an erased subject's refused message readable as work somebody can still act on, and directing that send fires an emptied message at an emptied address list from a system that has just certified the data destroyed. Written here rather than through consent's own closer because privacy may import neither consent nor approvals, and an erasure is one destructive transaction that must not depend on a seam a deployment could leave unwired: a review left live because a port was nil is an Art. 17 request half-done",
	"internal/modules/privacy:approval:scheduledsends.go:closeReviewsForErasedMessages:UPDATE approval …":                                                                               "the approval card a routed review was staged as is expired IN THE SAME STATEMENT that closes the review, so a review can never be closed while the card asking about it stays answerable. redactStagedApprovals cannot reach this one: it finds cards by the subject they name — contact target, lead twin, or an address quoted in the payload — and a review card names none of those, because its payload is the review id and the intent id and never repeats who the message was to. Forced expiry rather than deletion, matching that sweep: a decider who had it open learns the question ended instead of finding an empty row",
	"internal/modules/privacy:scheduled_send:scheduledsends.go:redactScheduledSends:UPDATE scheduled_send …":                                                                            "a message the rep chose to send later holds the subject's address, subject line and body BEFORE any activity exists, so the activity-keyed scrubs cannot reach it — Art. 17 empties the payload and cancels a pending one in the same transaction as the rest of the cascade, because a scheduled send that survived it would arrive the morning after the erasure certified the data destroyed",
	"internal/modules/privacy:approval:erasure_approvals.go:redactApprovalsCitingActivities:UPDATE approval …":                                                                          "a proposal's evidence is a VERBATIM quote of the body the caller just nulled, and nothing else in either engine reaches it — a proposal read from a meeting targets the ACTIVITY, so the subject-match arms never fire. Keyed on what the evidence CITES, because once the body is NULL the quote is the only place those words still exist. Pending rows are expired in the same statement: a blanked card left decidable is one a colleague can still approve",
	"internal/modules/privacy:approval:erasure_approvals.go:redactStagedApprovals:UPDATE approval …": "a staged proposal naming the subject holds a whole composed message, and pending rows are EXPIRED rather than merely emptied — a blanked card still in the inbox is one a colleague can approve, running its effect against an empty payload, or one whose gates cannot refuse it because nobody is named to refuse on behalf of" +
		" — blanking the staged proposal on the subject's pending approvals",
	"internal/modules/privacy:approval:erasure_approvals.go:redactStagedApprovals:WITH withdrawn AS ( …": "a staged proposal naming the subject holds a whole composed message, and pending rows are EXPIRED rather than merely emptied — a blanked card still in the inbox is one a colleague can approve, running its effect against an empty payload, or one whose gates cannot refuse it because nobody is named to refuse on behalf of" +
		" — the same statement's second half, blocking the workflow runs those withdrawn approvals were holding",
	"internal/modules/privacy:transcript_read:transcriptreadings.go:purgeTranscriptReadings:DELETE FROM transcript_read WHERE activity_id = ANY($1)": "a reading of a transcript is a record OF a body — how many lines it addressed, which proposals came out of them — so it cannot outlive that body, and both destructive engines delete it in the SAME transaction that nulls the body. Its own schema means it to go by cascade (core 0245), but neither engine ever DELETES an activity: Art. 17 redacts the row in place and retention nulls its content, both because a timeline row is other contacts's record too, so that cascade has never once fired. Routing it through activities would leave the transcript committed as a tombstone while a reading still answered questions about how long it was",
	"internal/modules/privacy:workflow_run:erasure_approvals.go:redactStagedApprovals:) …":                                                           "the automation runs parked behind those withdrawn proposals are ended IN THE SAME STATEMENT: everywhere else a withdrawal reaches its parked run by riding the approval.decided event, this path emits none, and the expiry sweep cannot repair it because it scans for pending and these rows are already terminal — the run would wait forever",
	"internal/modules/privacy:workflow_run:erasure_approvals.go:redactWorkflowRuns:UPDATE workflow_run …":                                            "an automation run records what it planned and what it did, so its own payload quotes the subject — nothing is withdrawn here, the run is already finished and this empties the history it left behind",
	"internal/modules/privacy:agent_run:erasure_approvals.go:redactStagedApprovals:UPDATE agent_run …":                                               "an agent run parks in awaiting_approval behind a staged approval and is only ever resumed by an approval.decided event. Erasure withdraws that approval without emitting one, so the run would wait forever holding the payload just destroyed — pending carries the staged call's arguments, which for a send is the recipient and the body. Ended here, in the same transaction as the withdrawal, for the reason workflow_run is",
	"internal/modules/privacy:comms_outbound:deliveries.go:redactDeliveries:UPDATE comms_outbound …":                                                 "the send log stores a second copy of an outbound message's recipients, subject and body, so Art. 17 erasure and the retention erase action scrub it in the SAME transaction that scrubs the activity it belongs to — routing it through comms would let the timeline row commit as a tombstone while the delivery still served the whole message",
	"internal/modules/privacy:comms_outbound:erasure_restrict.go:redactDeliveryAddressing:UPDATE comms_outbound …":                                   "the send log stores a second copy of an outbound message's recipients, subject and body, so Art. 17 erasure and the retention erase action scrub it in the SAME transaction that scrubs the activity it belongs to — routing it through comms would let the timeline row commit as a tombstone while the delivery still served the whole message",
	// direct audit_log/event_outbox writers: these paths need columns
	// storekit's writer does not carry.
	// the non-production admin data-reset orchestration (compose, cross-module
	// by nature — it sweeps every module's workspace_id tables in one
	// transaction) must clear the workspace's staged events alongside the
	// domain rows it just deleted: event_outbox has no workspace_id column
	// (tenancy lives in the envelope) and no owning module's store call could
	// scope a delete to "this workspace's queued events" without compose
	// growing a dependency on every module it sweeps.
	"internal/compose:event_outbox:datasweep.go:clearWorkspaceOutbox:DELETE FROM event_outbox": "the reset orchestration clears this workspace's staged events in the same transaction as the domain sweep, so no relay ever wakes on an envelope pointing at a row the reset just deleted",
	// identity owns login/failed-login, which land in system_log (a login
	// mutates no record). They fire before/without an authenticated
	// principal for storekit.LogSystem to stamp — bootstrap and failed
	// logins have no session yet — so identity appends the append-only rows
	// directly.
	"internal/modules/identity:system_log:installation.go:createInstallation:INSERT INTO system_log (actor_type, actor_id, action, detail) …":   "the installation bootstrap writes its own ledger row: it runs before any principal exists, so storekit.LogSystem has nobody to stamp as captured_by",
	"internal/modules/identity:system_log:lockout.go:Service.recordFailedLogin:INSERT INTO system_log (actor_type, actor_id, action, detail) …": "login and failed-login land in system_log but fire before/without an authenticated principal for storekit.LogSystem to stamp; identity appends the append-only rows itself",
	"internal/modules/identity:system_log:reset.go:OperatorResetPassword:INSERT INTO system_log (actor_type, actor_id, action, detail) …":       "an operator reset writes its own ledger row: the operator acts out of band and is not an authenticated principal of this workspace, so storekit.LogSystem has nobody to stamp",
	"internal/modules/identity:system_log:service.go:logAuthEvent:INSERT INTO system_log (actor_type, actor_id, action, detail) …":              "login and failed-login land in system_log but fire before/without an authenticated principal for storekit.LogSystem to stamp; identity appends the append-only rows itself",
	"internal/modules/identity:system_log:ssologin.go:Handlers.logOidcFailure:INSERT INTO system_log (actor_type, actor_id, action, detail) …":  "a refused federated sign-in has no authenticated principal for storekit.LogSystem to stamp either — same posture as recordFailedLogin (lockout.go), just for the OIDC login path instead of the password one",
})

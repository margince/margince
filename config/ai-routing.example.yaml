# yaml-language-server: $schema=./ai-routing.schema.json
# ai-routing.example.yaml — the tier→model binding's SHAPE, and the reference
# copy of it (ai-operational-spec §1.4).
#
# An installation's own binding is not a file: it is the `ai.routing` setting,
# declared for a fresh install under `seeds.ai_routing` in margince.yaml and
# changed on a running one under Settings -> AI. `api --ai-routing <path>` and
# `worker --ai-routing <path>` are IGNORED and warn — the flags stay registered
# only so an existing command line still parses.
#
# This file is still read, deliberately, by the lanes that probe a binding with
# no database open: `worker siteread`, `worker aitask`, and `make e2e-ai`, which
# defaults MARGINCE_AI_ROUTING here so a certification verdict is comparable
# between engineers rather than being about whatever each had bound. Nothing
# copies it to a per-engineer config/ai-routing.yaml any more.
#
# Field-level docs live in the sibling
# ai-routing.schema.json; the parser rejects unknown keys and unknown tier
# names at STARTUP, never on the first model call.
#
# provider — fake | anthropic | ollama | vllm | openai_compatible | openai | gemini
#
# env — there is NO api_key field. A cloud provider's BYOK key is read from its
# conventional environment variable at boot (ADR-0020); a stray `api_key:` line
# is a startup error. Set it in .env.local (`make dev` loads it) or the process
# environment (prod):
#   gemini            → GEMINI_API_KEY
#   anthropic         → ANTHROPIC_API_KEY
#   openai            → OPENAI_API_KEY
#   openai_compatible → OPENAI_COMPATIBLE_API_KEY
#
# input — NOT set here, and it does not need to be. `input:` declares what a
# bound model can be GIVEN (a scanned invoice, a photographed order form).
#
# Every tier below is gemini, whose wire carries image/* and application/pdf as
# a fact about the wire — declared in the adapter, true for every gemini model,
# nothing for this file to say. Documents already work on this binding.
#
# Setting it here would NARROW: on a provider whose carriage is fixed in code,
# `input:` means "at most this". `input: [text]` on a gemini tier is how an
# operator keeps scanned invoices out of an egressing model while keeping that
# model for text — per tier and per modality, which the `profile` ladder cannot
# express. It can only take carriage away; no declaration can hand a wire a lane
# it does not have.
#
# Re-point a tier at openai_compatible (OpenRouter, a self-hosted vLLM, an EU
# gateway) and the answer stops being knowable from code — there the declaration
# is the WHOLE answer, and that tier needs `input: [text, image]` or it will
# refuse documents however capable its model is.
# config/ai-routing.openrouter.example.yaml is the worked example, and
# docs/reference/configuration.md has the rules.
profile: eu_hosted

tiers:
  local_small:
    provider: gemini
    model: gemini-3.1-flash-lite

  cheap_cloud:
    provider: gemini
    model: gemini-3.1-flash-lite

  # premium serves the deep site read's extraction FIRST (site_extract is
  # premium-first in tasks.go) and its evidence gate demands verbatim quotes, so
  # a cheap-tier model here makes the read come back thin. Bind a mid-tier model
  # at minimum; judge a candidate with `make -C backend e2e-siteread`.
  premium:
    provider: gemini
    model: gemini-3.5-flash

  # frontier is the rung above premium: the strongest model this deployment is
  # willing to pay for. NO task ladder selects it — binding it costs nothing
  # until you put it in one (a routing-POLICY change in backend/api/ai-tasks.yaml,
  # which needs a rebuild). It is here so the rung has a measured default when a
  # task does need it, and so `make e2e-ai` can judge a candidate against it.
  # gemini-3.1-pro-preview is $2.00/$12.00 per MTok against premium's
  # $1.50/$9.00 — about a third more per token, which is the SMALLER half of
  # what a ladder change costs. Measured on the drafting fixtures (2026-08-12):
  # Pro emits roughly 18x the output tokens of cheap_cloud's flash-lite and
  # takes ~30s where premium takes ~7s and flash-lite ~2s. Token counts are not
  # equal between rungs, so price the change on measured output, and on an
  # interactive surface treat it as a latency decision first: 30s is felt by a
  # rep waiting on a draft in a way that a cent is not.
  frontier:
    provider: gemini
    model: gemini-3.1-pro-preview

# embeddings — bound separately from the chat tiers so retrieval keeps working
# when the chat budget is exhausted. Required.
embeddings:
  provider: gemini
  model: gemini-embedding-001
  dimensions: 1536

# `src/design-system/` — the Margince design system

Read this before you hand-roll a control.

## The rule

**Every interactive control comes from this directory.** A native `<select>`, a
hand-rolled dropdown, a bespoke menu, a second modal, one more "just this once"
chip — each of those is a defect, not a shortcut. Two reasons, and the second is
the one that bites:

1. A browser-drawn control is drawn by the browser. `<select>` takes our tokens
   on its closed face and none of them on the list behind it (`option` is not
   stylable in any engine we ship to), so it reads as a hole in a product built
   entirely from these files.
2. A copy drifts silently. The primitives here exist because the same row was
   already hand-rolled in eleven places, each with its own gap, its own focus
   ring and its own idea of what a label is — and every one of them looked fine
   in review on its own.

If what you need is genuinely not here, add it **here**, with a story and a
spec, and it becomes the one spelling. Copy never lives in a primitive: words
arrive through props, translated by the caller with `t()`.

## Indigo says a machine did it

**`--ai` is the AI hue, and it is not decoration.** An indigo tint on a surface
is a claim about provenance: *this information, or this proposed action, came
from an agent rather than from a person.* Paint it where that is true and
nowhere else. A card tinted indigo because indigo looked good tells every reader
of that screen something false about who decided.

It is a family of its own for the same reason the status colours are: one
meaning per colour. `--accent` (deep emerald) is brand and PRIMARY ACTION,
`--success` / `--warn` / `--danger` are status, and neither of those is
available to mean "authored by a model". The split is stated at the top of
`tokens.css` and pinned by `tokens.test.ts`.

| Token | Value | Role |
|---|---|---|
| `--ai` | `#5b61d6` | The line and the mark: a solid border on a staged card the reader has focused or accepted, a focus ring, a hairline. The strongest of the four, so it is the rarest. |
| `--aiLight` | `rgba(91, 97, 214, 0.08)` | The GROUND under agent-authored text: staged cards, the workbench wash, a `badge-ai` fill. |
| `--aiMed` | `rgba(91, 97, 214, 0.3)` | The border of something staged. Usually `1.5px dashed`, which is half the signal (see below). |
| `--aiText` | `#3f45b0` light, `#9ba0f0` dark | Text and glyphs ON `--aiLight`. Never `--ai` for text over the tint: the two are close enough that it fails AA on the ground its own family paints. |

**Dashed means proposed; solid means real.** `1.5px dashed var(--aiMed)` is the
convention for a thing an agent has staged and a person has not yet accepted:
`.staging-card`, `.deal-card.staged`, the deck's empty and staged slots. The
dashes going solid is what acceptance looks like. So the tint says *who*, and
the stroke says *whether it counts yet*, and those are two facts a reader needs separately.

**The provenance triad** (`trust.css`) is the rule applied at its smallest:
`.provenance-agent` is indigo, `.provenance-human` is teal, and
`.provenance-system` is plain ink on the card ground, because an installation's
own scheduled housekeeping is neither. `.provenance-unknown` borrows nobody's
colour and reads as the absence it is. A sweep drawn in the AI colour would tell
a reader a model decided something there.

### The orb is the same claim, made loudest

The agent Core and the lit window edge draw in the Core palette, and its working
tones are the AI indigo: `--orbBody` is declared as `var(--ai)` so the two cannot
drift, with `--orbGlow`, `--orbMid` and `--orbBright` around it. It is a family
rather than three uses of `--ai` because a lit glass body needs tones a flat UI
accent cannot supply: a light end to glow, a bright end for the one state with
energy, a dark to be seen against.

These were greens sitting around `--accent` until 2026-08. What that bought was a
re-themed brand that moved the orb with it; what it cost was the thing worth
more, a reader learning ONE colour for "a machine did this" and then meeting the
loudest machine on the surface wearing another.

The tones are named by ROLE, not by hue, and that is the second half of the same
lesson: they were `--orbMint` / `--orbJade` / `--orbLime`, and a token called Jade
holding an indigo lies to whoever reads it next. A role name survives the next
repaint.

**The three states that are not work keep their own hues.** `--orbAmber` asks a
person for something, `--orbRed` failed, `--orbGrey` cannot reach a source, and
none of them goes indigo. Provenance and outcome are different questions: an orb
that turned indigo when a run failed would say who was working and stop saying
how it went. `--orbRed` is also deliberately not `--danger`, which is UI chrome
and goes muddy at 34px.

**Colour is never the only signal.** Same standing rule as `Callout`: the words
carry the meaning. An agent-authored surface says so in text (a
`.provenance-agent` chip, a "suggested" label, an "Approve" verb), and the tint
is what makes it findable at a glance for the readers who can see it.

## What this directory already gives you

| Primitive | For | File | Story |
|---|---|---|---|
| **`Button`** | **The one button: `primary` / `ghost` / `danger` / `federated`, `small`, `iconOnly`. It owns its GEOMETRY, which is why none of it arrives as a prop: the height is `--control-h`, shared with `TextInput` and the `Select` trigger so a button beside a field sits on the field's line; an icon child is sized by the button (16px, 14px small) rather than by a `size=` at the call site, because lucide's own default is 24px and 31 call sites shipped that way; and a labelled button carries a width floor so "No" and "Add" still read as buttons. `iconOnly` is the one opt-out — square, no floor, and the caller still owes it an accessible name. `reason` / `reasonId` refuse it and say why, and the caller cannot defeat either by passing `disabled` or an `aria-describedby` of its own. `pending` is the OTHER unavailability and must never be spelled as the first: a write in flight sets `aria-disabled` rather than `disabled`, so the reader keeps focus on the control they just pressed, and it draws in full ink with a turning mark instead of the dimmed, barred treatment that everywhere else means “not yours to do”. The label does not change while it is set — `busyLabel` is where a sentence about the wait goes, because it lands in `aria-describedby` rather than renaming a control the reader is focused on. Both spellings of refusal outrank it: a control nobody may press cannot also be mid-press. `federated` is the sign-in door — full width because it is a door rather than a verb in a row of verbs, unfilled because a provider's sign-in is not this page's action to claim, another company's `ProviderMark` leading a label the INSTALLATION wrote, on the same `--control-h` target as everything else. The auth screen kept this as its own hand-rolled control for a long time on an argument that was half right — reusing `ghost` there meant overriding every one of its properties, and reuse that survives nothing is a claim rather than reuse. The answer to that is a VARIANT, not an exception. `unavailable` arrived with it and is the RESTING refusal: `disabled` is a precondition that clears, `pending` is a wait of seconds, and this is a door drawn because the installation offers it with nothing behind it — a deeper fade, refusing the press by itself, and carrying no sentence, because the surface that needs it may have nothing it is allowed to say. Neither dim recolours or grayscales the mark: fading a control of ours is ours to do, another company's logo is not** | `atoms.tsx` | ✅ |
| `BusyMark` | The turning mark a control shows while a write it started is in flight. Sized by whatever hosts it, and decorative — `aria-busy` on the control is the fact, so a glyph that announced itself would say it twice. Under reduced motion it stops turning and stays visible: a spinner has no end state to land on, and removing it would leave a reader who asked for less motion with no signal at all | `atoms.tsx` | via `Atoms → Buttons` |
| `.iconbtn` | A bare icon affordance that is NOT a `Button`: no fill, no border, no label — the verb a row offers where a full button would out-shout the row. A class rather than a component, because it is chrome a row applies to its own `<button>`; it carries the `--control-h-sm` hit target, the hover, and the focus ring | `base.css` | via `Atoms → Buttons` |
| `.card-actions` | A card's TRAILING action row: the verbs under a card's body, with the air above them and the gap between them. Not `.form-actions`, which is a form's submit row and carries no top margin because the field above it brings its own — a card whose body is a `FactList` and prose has nothing that does (`.factlist` sets `margin: 0` on purpose), so its buttons sit against the last line of text and against each other. The spacing rides on the row so it holds for every body the card can have above it | `atoms.css` | via `Attention` |
| `.cell-actions` | A TABLE ROW's verbs: the buttons in a table's trailing cell, right-aligned on one line. Not `.card-actions`, which is a CARD's trailing row — inside a cell its top margin pushes the buttons off the row's baseline, and the wrapping it allows turns two short verbs into a two-line cell that doubles the height of every row in the table. So no margin and no wrap; a cell too narrow scrolls with the rest of the table inside `TableScroll`, which is the answer that table already has. The labels belong in a table's register too: a verb column wants a word, not the sentence a confirm dialog carries | `atoms.css` | via `Senders` |
| `.link-button` | A secondary TEXT affordance — accent-coloured, no fill, no border — for an action that must not compete with the primary button beside it: a download, a "view existing", an in-row verb that is a link rather than a command. A class, like `.iconbtn`, because it is chrome a caller applies to its own `<a>` or `<button>`. **Not `.co-rowlink`**, which is for a row TITLE whose position already says it is a link and which therefore only underlines on hover — inside a form or a toolbar that reads as a value somebody typed. It carries its own hover and a solid accent focus outline; the outline is solid rather than the filled controls' low-alpha shadow ring because this class has no fill for the ring to read against. A lucide glyph may lead the label — the class sizes it to 16px and puts the pair on one line, exactly as `.btn` and `.iconbtn` do, so nothing about an icon is the caller's to spell | `atoms.css` | ✅ |
| `OffsiteLink` | A destination OFF our origin, drawn as a link when the address is one we may follow and as plain TEXT when it is not — the reader keeps the fact and loses only the link, exactly as `Chip` settled it. It owns the three things every hand-rolled `<a target="_blank">` was spelling for itself and some were getting wrong: `rel="noopener noreferrer"` (four sites carried `noreferrer` alone, which engines mostly imply opener-blocking for and none promise), the scheme check through `webUrl` — an href is untrusted record data and this is not a second authority on it — and what a refused address draws. Wears `.link-button` by default; a surface that styles its own anchors passes its class. **Named for the destination, not the glyph**: `ExternalLink` is lucide's icon of that name and four screens already import it, so a component sharing it would collide on every screen that wanted both. Not `Button` (a link goes somewhere, a button does something) and not `Chip` (that is an attribute with an icon naming its kind; this is a bare address in a value column) | `offsitelink.tsx` | ✅ |
| `Badge` | A status pill in the six tones. `quiet` is the same status without the fill — a dot in the tone's colour and the label as plain text — for a column that carries one per row, where a stack of pills reads as decoration a reader learns to skip | `atoms.tsx` | ✅ |
| **`Avatar`** | **A person's or company's chip: monogram, optional logo, and a tint that is ALWAYS drawn. The tint was opt-in once, and the result was that a company was a coloured chip in the list it was found in and a neutral accent chip on the record page that list opened — one click apart, same company. `identity` is what the tint is keyed on when the displayed name is not stable (a record id, an address): without it a rename moves a record to a different colour on every screen at once, which reads as a different record. `size` is the whole scale — `xs` dense table, `sm` list row (the default), `md` record header, `lg` wide record header — because four sizes were being rendered by three stylesheets for a prop that admitted two. The monogram splits on address punctuation as well as whitespace, so a reader known only by `jane.doe@…` is "JD" rather than "J"** | `atoms.tsx` | ✅ |
| `AvatarStack` | A group of people as overlapping monograms, folding past `max` into a "+N" chip rather than running the row wide. Expects a non-empty list: every caller guards `people.length > 0` before rendering it, so there is no "0 people" state to draw. Its separating ring is a `box-shadow`, not a border, so a stacked face is the same size as the lone chip beside it | `avatarstack.tsx` | ✅ |
| `TextInput` | The one text field | `atoms.tsx` | ✅ |
| `SearchField` | A text field with the search affordance | `atoms.tsx` | ✅ (`Atoms → Search`) |
| `Textarea` | The one multi-line field | `atoms.tsx` | ✅ |
| **`RichText`** | **The light formatting a business email needs and nothing more: bold, italic, links, lists.** Deliberately `contentEditable` rather than an editor library — the server's outbound allowlist (`activities.SanitizeOutboundHTML`) already normalises what a recipient receives, so a toolbar offering headings or colours would offer formatting that never arrives. Every change reports BOTH `html` and `text`: they are one message in two forms and both go on the wire, so a caller keeping one would send halves that disagree. No image button — a remote image is a read receipt | `richtext.tsx` | ✅ |
| **`Select`** | **The one dropdown: a button trigger plus a portalled listbox. Never a `<select>`.** An option whose label is not in the page's language carries `lang` (a BCP 47 tag), which reaches both the option and the trigger face — WCAG 2.2 AA 3.1.2, and the reason a language picker is buildable here at all | **`select.tsx`** | ✅ |
| **`ComboBox`** | **The one field that both TAKES a value and offers some: a text box over a portalled listbox, where the list is help and never a constraint.** Reach for it over `Select` when the known answers are a good starting set and the real vocabulary belongs to somebody else — a model id, where the price sheet knows what this installation can cost and the vendor ships a new one on a Tuesday. The value is whatever is in the box: typing is never overridden (`aria-autocomplete="list"`, never `both`, which rewrites what a reader is halfway through typing), Escape closes the list and keeps the text, and with nothing to suggest the whole popup apparatus stands down and this is an ordinary input — no chevron promising a list that is not there. Shares `anchoredpopup.ts` with `Select`, because where a dropdown may sit on a page is one answer, not two | `combobox.tsx` | ✅ |
| **`ProjectPicker`** / **`ScopeLine`** | **The one way a surface is told which project it is about, and the one line that says which project its output was narrowed to.** `ProjectPicker` is a `Select` over the `projects` section the two 360s share (`liveProjects` drops the closed ones; `useSoleProjectDefault` picks the only live one once), and under it `ScopeLine` prints the server's own report — "Scoped to KEY · N of M activities" — or the key alone while no counted report names the chosen project. Every AI surface renders this pair: the two composers, the prepared questions, the meeting brief. A second picker is a second answer to "which body of work is this about" | `projectpicker.tsx` | ✅ |
| **`DateInput`** | **The one date field.** A native `type="date"` — the deliberate exception to the `Select` rule, because a date input's CLOSED FACE is an ordinary text box that takes our tokens completely, and what the platform draws is only the picker popover, which appears on request and which users already know how to drive. It owns the FORMAT: `value` is always `YYYY-MM-DD`, the same shape the contract's `format: date` fields carry, so a value round-trips wire→control→wire with no parse step to get wrong | `dateinput.tsx` | ✅ (`Value inputs`) |
| **`TimelineFilterBar`** | **The one row of dials above a record timeline: a kind `Select`, a `SearchField`, and a from/to pair of `DateInput`s.** Every dial is a SERVER parameter (`kind`, `q`, `occurred_after`/`occurred_before`), spelled for the wire once in `recordtimeline.ts` (`timelineQueryParams`); the bar owns no list and filters nothing itself, which is what keeps "tasks on this contact" from meaning "tasks among the twenty newest rows". Its state is `TimelineFilters`, held per record by `useTimelineFilters` and handed to `useRecordTimeline`, the paged read every record page (person, company, deal, lead, project) draws its timeline from. The search commits on Enter or blur, one request per commit. While a search is active the bar says that limited conversations are left out — a text match reads content, so the server answers it content-gated | `timelinefilterbar.tsx` | ✅ |
| **`TokenInput`** | **A set of short values built one at a time — the control the filter engine's `in` operator needs, where the operand is a LIST (`region ∈ {DE, AT, CH}`) rather than a scalar.** Not a multi-select `Select`, because `in` is used where the candidate set is not enumerable in advance (nobody can list every city). Enter or comma commits, Backspace on an empty box removes the last, a duplicate is dropped silently (`in` is a set, so a refusal would be noise), and blur commits so clicking away never discards what was typed | `tokeninput.tsx` | ✅ (`Value inputs`) |
| **`TokenList`** | **The same token as `TokenInput`, without the text box: a set assembled somewhere else — by a picker, a search, a selection in another control — drawn as a set rather than as a run of sentences.** Same file and same stylesheet on purpose (one token, one home); it takes `{id, label}` because the value a caller stores is not the words a reader should see. `removeLabel` is a function so each control's accessible name carries WHICH token, and omitting `onRemove` renders a read-only set rather than disabled buttons | `tokeninput.tsx` | ✅ (`Value inputs`) |
| `Checkbox` / `Radio` | A tick with its label as the other half of the click target | `atoms.tsx` | ✅ |
| **`Field`** | **The label-above-control row every form is built from. Owns the id and hands the control `{ id, required, aria-describedby, aria-invalid }`, so a call site never mints an id or repeats one. Four slots beyond the label: `hint` is the rule that always applies, `error` is a refusal — its own slot, because putting a refusal through `hint` renders it in the same meta-grey as neutral help and, on one card, as the SUCCESS line four elements above it — and `icon` / `trailing` are affordances INSIDE the control's outline, which is what `auth.tsx` forked its own field component to get. The shell appears only when `icon` or `trailing` is passed, so a plain field's markup is unchanged. `labelEnd` puts a "Forgot?" beside the label without it joining the control's accessible name** | `atoms.tsx` | ✅ (`Atoms → Fields`, and `Fields — affordances and refusal`) |
| **`usePasswordReveal`** | **A password field's reveal control AND the input `type` that goes with it, returned together because they are one fact — a caller handed only the button holds the other half itself and can get the two out of step, which is a control announcing the opposite of what it does. Pass `trailing` to `Field` so it sits inside the focus ring. Labels arrive translated, like all copy here** | `passwordreveal.tsx` | ✅ (via `Atoms → Fields — affordances and refusal`) |
| `FieldGrid` / `FieldRow` | The two-column label/value grid around a record's fields: the grid AROUND a value, not the value itself. Fixed-width label column (not `auto`), so every value's left edge sits at the same x on every record and every locale — a `FieldRow`'s `label` is always required and always drawn there. A read-only row takes a plain node; an editable one wraps `InlineText`/`InlineChoice` as `FieldRow`'s children rather than this component reimplementing hover-to-edit. `InlineText` draws no visible label of its own (its `label` prop is screen-reader- and aria-only), so pass `FieldRow`'s own `label` as usual there. `InlineChoice` DOES draw its own "label: value" inline, closed or open — pass it `hideLabel` to suppress that visible half so `FieldRow`'s label is the only one on screen, while the value still sits in the grid's shared value column rather than escaping it | `fieldgrid.tsx` | ✅ |
| `MoneyInput` | An amount with its currency, formatted at the presentation edge | `moneyinput.tsx` | ✅ |
| **`SettingList` / `SettingRow`** | **One settings DECISION per row: what it is and what it does on the LEFT, what it is set to on the RIGHT, at one x down the whole page. Reach for it in every settings card — the shape is what lets a reader audit a page by travelling one column instead of finding the answer somewhere new in each card, which is what this tree had. `control` takes a node OR a function; the function form receives `{ id, aria-labelledby, aria-describedby }` so the label the row draws and the name the control announces are the same string with the caller writing it once (a `Switch` keeps its own `labelHidden` label, because it owns its NAME by design, but still takes the row's `aria-describedby` through `describedBy` — otherwise the sentence saying what the setting does reaches nobody who cannot see it). `value` is the current answer standing beside the verb that changes it — "marek@gradion.com [Edit]" — and is passed ONLY where the control does not already show it. `layout="stack"` gives the control the full width below the naming, and what picks it is COMPLEXITY, not size: a control that ANSWERS the row's question sits beside it, a control that IS the subject (a table, a toggle matrix, a log) goes below, and a control needing two inputs submitted together belongs behind a verb in a `Modal` so the row stays an answer. The hairline between rows rides on the list, not on each row — a row that spaced itself would space itself differently in the next card** | `settingrow.tsx` | ✅ |
| **`FileDropzone` / `FileDropzoneControl`** | **Choosing ONE file, by drop and by click, from a single control. The `<input type="file">` is the control and the zone is chrome stretched over it, so the keyboard path is the real one rather than a handler imitating it — a bare drop target excludes everyone not holding a mouse. Nothing else in the zone is interactive and only the label text is `pointer-events: none`; inheriting that onto the input made the whole zone unclickable while every jsdom test still passed. `emptyLabel` belongs to the caller because only the caller knows what it is asking for. An empty selection never fires `onPick` (cancelling a picker is not the act of clearing a field), and the input's value is cleared after every pick, or re-choosing the same path fires no event at all. `accept` filters the PICKER only; a drop is never filtered by it, so a caller that reads some types still checks what it was handed. **`FileDropzone` brings its own `Field`; a caller that renders more inside one — the contract form lists the paper already filed above the zone — takes `FileDropzoneControl` and passes the `Field`'s control props down, so one input keeps one label** | `filedropzone.tsx` | ✅ |
| `SegmentedControl` | A small closed set of options, all visible at once. `counts` puts how much is behind each option beside its name, for a strip that chooses between bodies of a RECORD rather than between settings — partial and per-option, because an option that is not a list of things (a 360, a form) must draw no figure, and that absence is NOT a zero: a zero is a fact about the account and prints, a missing count is a fact about the section and does not. It renders INSIDE the button, so the figure joins the option's accessible name instead of being left to a sighted reader alone. A caller owes it only counts it can state truthfully — a paged section whose `has_more` is set has a floor, not a count, and passes none. `marks` puts a dot on options with something waiting behind them (a tab whose surface holds an untaken action) instead — the dot is `aria-hidden` and never the only carrier of the fact, because a mark is what a screen reader cannot read and a colour-blind reader may not see | `atoms.tsx` | ✅ |
| **`ChoiceList`** | **One question, every answer readable AT REST — the radio GROUP the `Radio` atom never had. A native `fieldset` and `legend` with NO `role` on top (the browser already exposes the grouping of native radios, and an ARIA role would take the element out of the mapping that gives the `legend` its meaning), a legend that can be hidden without going unnamed, and an optional `description` per answer that is part of the label (so the help line is clickable and is read with the option it belongs to).** Reach for it over `Select` for a decision a reader must weigh — a dropdown over two options makes somebody open a menu to discover the alternative — and over `SegmentedControl` the moment an option is a sentence rather than a tab name | `choicelist.tsx` | ✅ |
| **`Switch`** | **A setting that writes when you flip it. A `Checkbox` states an intent something later submits; this IS the action, which is why it announces `role="switch"` and takes a `pending` state — the same refusal semantics as `Button`'s, `aria-disabled` and a mark rather than a dimmed control, with `disabled` and `reason` both outranking it. `reason` refuses the flip on its own and no `disabled={false}` beside it takes that back, exactly as on `Button`: it is the sentence for a change this reader may not make, so the control under it is one they cannot flip. `describedBy` names an element the CALLER owns, alongside the switch's own hint and refusal rather than instead of them: inside a `SettingRow` the sentence saying what the setting does is the row's `description`, and a node-form control cannot see the id the row minted — so a row holding a switch passes it through the function form of `control`. No `busyLabel` here: no surface has needed one, and a prop with no caller is an API nobody has tested. A filter over a list is neither — that is a pressed button** | `switch.tsx` | ✅ |
| **`Callout`** | **What a surface says about ITSELF, in four closed tones: `info` carries no urgency, `warn` says something will go wrong if you do nothing, `danger` says something is wrong or about to be irreversible, `success` confirms it landed. Never content, and never the only signal — the words carry the meaning** | `callout.tsx` | ✅ |
| **`FactList`** | **Label→value pairs a reader scans rather than edits. Rows arrive as an array so a caller drops absent facts: an empty value claims we know it and it is blank, which is not the same as not knowing** | `factlist.tsx` | ✅ |
| `ProjectLinks` | **The ONE section a record shows its projects in, and the one place a reader attaches or detaches one.** A deal, a company, a contact and a project itself all answer "which bodies of work is this part of", and before this they answered it three ways — a picker on the deal form, a read the company and contact pages used only to fill a brief, and no attach or detach anywhere. The record page supplies an ADAPTER (what is linked, how to link, how to unlink, whether a second link is allowed); the surface, verbs, empty state, confirm and refused-write states are the component's. `allowsMany: false` turns "Attach" into "Move", because that is what the write does. The project page uses the SAME control for its companies, with the role standing where a phase would | `projectlinks.tsx` | ✅ |
| `RecordPicker` | Search → candidates → pick, for choosing an existing record | `recordpicker.tsx` | ✅ |
| `PassportSelect` / `ScopeChips` | Which agent passport, and the scopes it carries | `passportselect.tsx` | ✅ |
| `Modal` | The one dialog: portalled, Escape-closing, Tab kept inside. `placement="right"` is the drawer form — full height on the right edge, the record behind still legible; on a phone both are the same full-screen sheet | `atoms.tsx` | ✅ (`Dialog` centred, `Drawer` right) |
| `ConfirmModal` | A dialog that asks before something irreversible. It owns the heading, the error line and the Cancel/Confirm pair, so a caller reaching for a bare `Modal` and writing its own `h2` plus a flex footer has written this component again — three lead dialogs had. `confirmDisabled` refuses the confirm as a STATE; `confirmReason` refuses it with a SENTENCE, which is what a precondition the reader could meet needs — `Button`'s own `reason` disables the control, prints the line beside it and wires `aria-describedby`, and a dialog whose confirm is dead with no explanation is a dead end. Pass one or the other, never both | `confirmmodal.tsx` | ✅ |
| `NamePrompt` | A write whose only input is a name: trigger, dialog, name box, save. It owns the name, the rule that an empty one cannot be saved, and clearing the box on success — the chrome is `ConfirmModal`'s, which is what keeps it from being a second dialog. `onSave` takes a `done` callback rather than returning a promise, because a refused write must leave the dialog open with what was typed still in it; the caller calls `done` from its own success path. Reach for it instead of hand-rolling a `Modal` with a `Field` — the saved-view action did that, and the second surface needing the same question would have asked it differently | `nameprompt.tsx` | ✅ |
| `OverflowMenu` | The verbs a record offers but a reader rarely wants | `atoms.tsx` | ✅ |
| `Disclosure` | A section the reader opens when they want it | `atoms.tsx` | ✅ |
| `Popover` | A short aside on CLICK, portalled beside its trigger and gone when the reader looks away. Reach for it where a `Disclosure` would push the page around — a receipt under one card in a row of readings, where opening one moved the three beside it out from under the eye comparing them. Not `useTruncationTooltip`: that appears on hover, says one line, and a touch reader never sees it; this holds a paragraph or a small list and can carry a link. Closes on Escape (handing focus back to the trigger) and on a click outside BOTH the trigger and the panel | `popover.tsx` | ✅ |
| `Calendar` | A month of days with one marked, for a date chosen INSIDE a decision the reader is already making — a send being scheduled, where which weekday it lands on is the point. Everywhere else the native `DateInput` is the control: it is the reader's own platform's, it speaks their locale, and it costs nothing. Presentational — the month on show and the day chosen both belong to the caller, and `today` is passed in, so two calendars on one screen cannot disagree about what day it is. Six weeks always, so the controls under it do not move as the reader pages | `calendar.tsx` | ✅ |
| `FilterPills` | The cuts THROUGH a list, one of which is in view — All / Conversations / Changes. Each pill is its own outlined object, because each is a separate question about the list; `SegmentedControl` is the one to reach for when the options are a SETTING and exactly one of them is on. `count` is optional per pill and absent draws no figure, which is NOT a zero: a paged list whose `has_more` is set knows a floor, and a floor printed as a count is a wrong number | `filterpills.tsx` | ✅ |
| `RecordTabs` | The strip that chooses which body of a record is open, drawn as a rule with the current tab underlined rather than as a pill. A record has eight bodies of unequal size and the strip is a place a reader navigates, not a switch they flip; the hairlines run the full width of the work column because they divide the record's IDENTITY from its CONTENT. `counts` is partial and per-option on the same rule `FilterPills` follows | `recordtabs.tsx` | ✅ |
| **`Card`** | **The one card surface. `as` picks the element (`section` by default, also `div` / `article` / `form` / `li`), `inset` is the recessed variant, and `title` / `sub` / `actions` render its `SectionHeader` — a hand-rolled `<div className="card">` is a second card the moment one of the five chrome values moves** | `atoms.tsx` | ✅ |
| `SectionHeader` | The heading of a block: title, description on its own line under it at full width, and the block's actions beside the pair. `level` is `1` / `2` / `3` — `1` for the one header that IS the page's name, `3` for a section INSIDE a section, and the type steps down with the outline rather than leaving a nested heading the same size as its parent. `Card` passes it straight through | `atoms.tsx` | ✅ |
| `EmptyState` / `Skeleton` / `Kbd` | Page furniture: nothing-here, one placeholder bar, key cap. `EmptyState` has two faces on one plate: bare, it is the one-line "nothing here" a filtered list or an empty section shows; with `title` it is the INSTRUCTIONAL first-run state — heading, the caller's paragraph saying how a record of this kind comes to exist, and the one `action` that creates the first one (the projects list is the worked example). The verb renders only with the title, because a bare one-liner offering to create what a filter hid would be a false invitation. `Skeleton` is the BAR — reach for `PendingBody` below unless you are drawing a shape (a ring, a chart) that a column of lines cannot stand in for | `atoms.tsx` | ✅ |
| **`PendingBody`** | **The one pending state. Four grew before it — three inline-styled bars in `QueryStates`, one silent 32px bar in `SurfaceState`, five unanimated bone rows in `ListTable`, and a long tail of hand-rolled bars and bare "Loading…" lines — and they disagreed about the shape, the height, and whether anything was ANNOUNCED at all. `label` is REQUIRED and not defaulted: a placeholder carries no text, so the spoken line is the only thing a screen reader has, and it is the caller who knows what is being waited for ("Loading the review queue…" beats "Loading…" for somebody who cannot see which part of the page went grey). `lines` is a HEIGHT RESERVATION, not decoration — under-reserving is why a card jumps when its body arrives, and the jump costs a reader their place, which is worse than the wait. `visible` shows the label as well as speaking it, for a wait long enough that a mute grey block reads as broken. `QueryGate` / `QueryStates` / `SurfaceState` all render it, and forward `pendingLabel` / `pendingLines` (`loadingLabel` / `loadingLines` on `SurfaceState`) so a screen names its own wait without spelling a second pending state. Two fitness functions hold the line: the announcement has exactly one home, and so does the pulse** | `atoms.tsx` | via `SurfaceState` |
| **`ToastProvider` / `ToastRegion` / `useToast`** | **The transient confirmation — "that worked" — fixed to the foot of the viewport. ONE provider and ONE region, mounted in `main.tsx` and portalled to the body; a screen only ever asks for `show`. It was a hook each screen mounted for itself, and all three failures of a global surface held locally were live at once: `commissiondecide.tsx` called it and rendered no region, so every confirmation went into a `useState` nobody read; `deals.tsx` carried a comment explaining that the instance belongs to the caller; and the region being a SIBLING of the page's cards forced two layout rules elsewhere to be written around a fixed box that takes no space. It withdraws itself after 3.5s — unless a pointer is over it or focus is inside it, which stops the clock and hands the full life back on leaving (WCAG 2.2.1). `action` is the verb it carries, drawn by the region rather than smuggled into the message, and it makes the toast sticky and gives it a way out: a reader reaching for Undo must not lose it mid-reach. A message carrying one is never evicted by one that only reports — that queues behind it, because a courtesy must not take away the reader's only route back. `{ mark: false }` drops the green completion dot for anything that is not a completion, which says the opposite of what the sentence says. The `<output>` is a live region already, so nothing beside it declares `role="status"`, and focus is never taken — the way in is Tab, the way out is Escape. WHICH writes get one is a policy, and it lives in the story** | `toast.tsx` | ✅ |
| `Panel` / `PanelBody` / `PanelRow` / `PanelPlate` | The record page's titled-card shape, which `Card` does not offer: a fixed-height header (a title alone, or a title with a badge or a button, all the same height), full-bleed rows under it, and an optional footer band for a figure that belongs to the whole panel. `sub` is one line of description under the title, in the same band — the header height is a FLOOR, so a description is the one slot that raises it while everything sharing the title's line keeps the one height. Reach for it instead of falling back to `Card` for the sake of a sentence: that trade is how a panel-shaped card ends up drawn as a second surface. `PanelBody` is its own component rather than a prop on `Panel`, because the header's rhythm, the body's padding and a row that wants to touch the panel's own edges are three different things living in one box: a caller needing both padded text and full-bleed rows nests `PanelBody` and `PanelRow` as siblings instead of fighting one slot that tries to be both. A `PanelRow` is INERT unless you pass `interactive`, which claims the WHOLE row is one press target — a button or a link filling it, so pointing anywhere in the row aims at the same thing, which is what earns the hover fill. A row that merely CONTAINS a control is not that: the control draws its own hover, and a fill behind it claims a hit area the row does not have. The hairline between two consecutive `PanelBody`s is the panel's own seam rule: stack `PanelBody`s to get ruled reading blocks, and let the panel draw that seam rather than adding a border of your own. `tone` is the LEAD variant: a tinted border and header band for the single card on a page that asks for a MOVE rather than reporting state — two tinted panels on one page is no lead at all. Its three values are three kinds of lead, not a palette — `"accent"` for the ordinary ask, `"warn"` for a lead whose FINDING is the bad news (gone quiet, a promise overdue), in the warn family's own tokens and the same pairing `Callout` draws, and `"ai"` for a panel a MACHINE wrote or read, in the `--ai` family. That last one is a claim about AUTHORSHIP rather than about the account, which is why it is not simply a third accent: indigo means "Margince did this" everywhere in the product, so it must never be reached for to make an ordinary panel look important. Only the four tinted properties differ between them; the lead's geometry is stated once, because a card that changed size when its finding turned bad would read as a different card. `actions` is a band under the body for verbs that CHANGE the panel (the footer reports; this acts). `PanelPlate` is the recessed plate inset from the panel's edges that separates what IS from what to DO: context on the plate, moves full-bleed on the panel's own ground, and a reader can tell the halves apart before reading a word of either. It is a `Card` with rows, not a rival surface — when `Card` grows a row and a footer band, this folds into it. `titleLevel` says which heading the title takes: `2` by default, `3` for a panel inside a dialog, where the dialog's own title is already the h2 and a panel claiming a second one puts two non-sibling h2s in the outline | `panel.tsx` | ✅ |
| `StatCard` / `AttainmentRing` | One reading with the basis it was drawn from; the server's attainment band as an arc. `value` is a **string**, and `numeric` is what gives a figure the mono face — money, a count, a duration, where digits of one width make a column of readings line up. Deliberately a flag and not a `ReactNode` value: widened, the face would be the caller's to spell, and a screen that spells type is the second author of a scale this tier owns. `basis` + `basisLabel` fold the rows the reading was COMPUTED from under it — a `Popover` over the card, opening to a settled pointer AND to a click or Enter, so comparing a row of readings costs a sweep rather than five presses and a keyboard still reaches every receipt. Both props or neither: an unlabelled receipt asks the reader to open it to find out whether they wanted it. `openLabel` + `onOpen` put the way OUT on the reading's own name line and repeat it at the foot of the panel — the tab that holds what the figure was read from; `meter` draws the share under the words as counted segments (few) or one filled track (many), and only where the reading HAS a denominator. `detail` still carries the one-line basis when `basis` is set, because a reader who never opens it must not meet a number with nothing behind it | `atoms.tsx` | ✅ |
| **`StatStrip`** | **A record's readings as ONE plate of ruled slots rather than N free-standing cards — cards are read one at a time, a strip is read across as a single comparison. Takes `StatCard`s as children and owns only the plate: slot count (from the children actually drawn, so a conditional slot leaves no empty cell), the rules between slots (drawn once by the plate, through the grid's own gaps, so no slot has to know whether it begins a row), the fold when the row stops being legible — the last slot takes the rest of its row, so a count that does not divide the columns leaves no orphan beside empty cells — and the one type scale every slot shares. A slot sized to its own content stops the row reading as one comparison** | `statstrip.tsx` | ✅ |
| `Meter` / `Sparkline` / `Chip` | A proportion as a bar (pass `value` and `max`, never a percentage), a short series as a bare polyline, and one attribute of a record as an icon pill — a `Chip` is a fact, a `Badge` is a status. `Meter`'s `tone` colours the FILL and `restTone` says the TRACK is itself a value the caller names beside the bar (overdue against open) rather than the empty space the fill has not reached; `dense` is its second size — the bar as a LABEL'S own bar rather than a block of its own, thinner and with no vertical interval, which is what a row-per-dimension reading wants. Take the size from the prop: reaching into `.meterbar` from a screen sheet for a height is how two callers became two authors of one geometry | `readings.tsx` | ✅ |
| `FileChip` | One stored file as the small card a reader clicks to get it: kind glyph, filename, same-origin download. Distinct from `Chip`, which is a fact about a record and links off our origin — this IS the file, so its name is the filename the saved copy will carry, and two files on one row are tellable apart before the click rather than after it. PDF is the one kind with its own glyph (it is what agreement paper arrives as); everything else takes the neutral mark rather than a guessed one | `filechip.tsx` | ✅ |
| `DataTable` | A simple column/row table with optional row navigation, drawn inside a `TableScroll`. `label` is REQUIRED: it is the name the scroll region announces once the table is wider than its box, so it is the same phrase the heading above the table already says (`t("aiusage.spendLabel")`, `t("offer.lines")`) | `atoms.tsx` | ✅ |
| **`TableScroll`** | **The ONE box a table too wide for its column scrolls sideways INSIDE, so the page never scrolls with it — 720px settings columns and eight-column delivery logs meet here. Reach for it for any hand-drawn `<table>` that `DataTable` cannot express; `DataTable` already renders into one. It takes a tab stop and announces itself as a named `region` only while it is ACTUALLY holding something past its right edge, because a tab stop in front of every table in the product is a cost every keyboard reader pays for the few that overflow. `label` is required for that name — an unnamed region tells a reader nothing about which of the page's tables they just landed in. Four screens had each written this wrapper by hand and none of them was reachable** | `atoms.tsx` | via screen stories |
| `EvidenceMark` | The ONE §4 provenance affordance: a dotted underline on a value a person did not type, opening to where it came from | `evidencemark.tsx` | ✅ |
| `AutonomyDot` | The 🟢/🟡 autonomy semantics as a token component — never an emoji glyph | `trust.tsx` | ✅ |
| `EvidenceChip` / `ConfidenceMeter` / `ProvenanceTag` | Where a value came from, how sure we are, who captured it. Inside `EvidenceMark` and on the staging surfaces — never stacked under a field. `EvidenceChip`'s `evidence.lines` adds WHERE in the source the snippet sits, as 1-based line numbers: consecutive numbers close into a range (`lines 12–14`) because that is one place in the transcript, gaps stay listed apart, and a source with no lines to point at renders exactly as before. `ProvenanceTag` draws one arm per KIND of actor — agent, connector, system job, human, buyer, and unrecorded — because telling them apart is the only reason it is on the screen; `agent`, `system` and `buyer` may arrive with no name, and then the tag states the kind rather than printing an opaque id (there are no record lookups here to resolve one). `buyer` is a Deal Room participant: a person, from outside the organization, holding no seat — its own arm because `human` would send a reader looking for them in the member directory and `unknown` claimed nothing recorded a source when a person did | `trust.tsx` | ✅ |
| `confidenceLevel` | The ONE spelling of the confidence bands: a wire 0..1 as the level `ConfidenceMeter` draws, high at 0.8 and med at 0.5, inclusive. Eight surfaces read it, and it lived in a screen file for months while they did — the defect this README names. Null in, null out: an unrecorded confidence is not a low one, and drawing it as low puts a claim on the screen the data never made | `trust.tsx` | ✅ |
| `ApprovalGate` / `StagingCard` / `StagedProposal` | Staged-not-real state and the Accept / Edit / Dismiss triad | `trust.tsx` | ✅ |
| `FieldDiff` | The inline old→new value diff; a null side reads as a marker, never a blank | `trust.tsx` | ✅ |
| `PassportChip` | An agent passport id, mono so it reads as an identifier | `trust.tsx` | ✅ |
| **`DecisionCard`** | **The ONE card that asks a person to decide something an automation staged, in two layouts and no more: `deck` (tall, one at a time, the whole payload) and `row` (the compact list form). It was `ApprovalRow` in a screen file with seven screens reaching into it, which is the defect this README names — and two things changed in the move. It RENDERS THE PROPOSED CONTENT: the old row said "an automation drafted a reply to <them>" and stopped, which is everything about the proposal except the proposal, so a `subject`/`body` payload now shows the draft and a `current_X`/`proposed_X` pair shows both sides through `FieldDiff`. And EXPIRY IS PRESSURE, not a caption — `expires_at` tints the card's own edge in three bands from `decisionUrgency` (the ONE home for the 1h/6h thresholds; a countdown badge reads the same function rather than a second copy), and past the deadline Accept is not drawn AT ALL, because a control whose only possible answer is a refusal is worse than no control. It carries `.staging-card` as well as its own class rather than redrawing that ground. No mutation, no query, no copy: verbs are callbacks, words are `labels`, and the chips only a screen can build (the autonomy dot, the kind, the countdown) arrive through `meta` / `aside`, the inline editor through `editor`, and a bundle's member list through `detail`. WHAT A KIND SHOWS arrives the same way, through `display`: a proposal's payload is an open map, so without a policy the card printed its own JSON keys and a person deciding whether a deal was still alive was handed `deal_id`, `flags: ["unrealistic_stale"]` and `target_version`. `screens/approvalkind.ts` declares per kind which fields are worth a caption and which one is the SENTENCE that leads the body, resolves them into the reader's language, and hands finished strings over — the kind catalogue is the product's vocabulary, and a primitive holding a copy would be its second author. A kind that declares nothing keeps the raw reading, which is honest rather than lazy: half the stageable kinds carry an agent's tool arguments with no typed payload to describe** | `decisioncard.tsx` | ✅ |
| **`DecisionStatusChip`** | **The decision's header chip: the verdict once it has been answered, else the live countdown to its deadline. ONE chip for every decision surface — Today's focus lane and Home's deck both draw it — because a second countdown is a second answer to one deadline, and the two drift the first time either moves. It reads `decisionExpiryMs` / `decisionLapsed` / `decisionUrgency` rather than re-deriving them, so its tone escalates on exactly the bands that tint the card's own edge. A lapsed pending proposal gets NO chip: the card already says it ran out of time where its verbs were. Carries no copy — `labels.expiresIn` takes the milliseconds remaining and hands back the sentence, because the units are translated strings too, and one verdict word per decided state; a status this tier has not learned reads as the lapsed word rather than as nothing. It takes a `DecisionDeadline` (`status` plus `expires_at`) rather than a whole `Approval`, and the one field that matters is `status: string`: the vocabulary grows on the server, so that fallback is a branch a build really does reach — the same call `screens/approvalkind.ts` made for `kind`** | `decisioncard.tsx` | ✅ |
| **`DecisionToolChip`** | **Names the tool that actually staged a proposal, so a reader can tell `send_email` (the tool) from `advance_deal` (the kind) without opening the detail. The verb arrives already looked up and the words to wrap it in arrive with it: the kind→verb catalogue is the product's vocabulary, not this tier's. No verb, no chip — naming a tool nobody can check is worse than silence** | `decisioncard.tsx` | ✅ |
| `decisionUrgency` / `decisionUrgencyTone` / `decisionExpiryMs` / `decisionLapsed` | The deadline vocabulary the card, its chip and every countdown badge on a decision surface read. `decisionUrgency` is the ONE home for the 1h/6h bands and `decisionUrgencyTone` maps them onto a `Badge` tone — a surface that spelled the hours again is how one deadline comes to read urgent on the card and calm in the badge beside it | `decisioncard.tsx` | ✅ |
| **`DecisionDeck`** | **The morning queue of staged decisions, answered one at a time. STAGE, THEN COMMIT is the whole design and not a flourish: a recorded verdict is un-undoable (approving mints a single-use token and executes the effect), so a surface that sent on the swipe would be one where a flick of the wrist sends an email. A swipe or a key stages LOCALLY, the tray shows what is waiting, and nothing leaves the browser until an explicit commit — which makes the tray the undo the backend does not have. Un-staging is free until that press. Four inputs, one vocabulary: drag right/left/up/down and `→ ← ↑ ↓` both go through `dragVerdict`/`keyVerdict` (`U` un-stages the last, `Enter` commits, and staging the last card hands focus to the commit control, because the keyboard surface leaves with the card). Two peeked edges and a count say what is still behind; a `bundle_id` group collapses into ONE decision with its recipients behind an expander, because the API decides a bundle as a unit; `SegmentedControl` switches to the list form, which is the DEFAULT under `prefers-reduced-motion` — a deck IS its motion, and one without it is a list drawn the expensive way. Motion is CSS transforms on the six `tokens.css` durations (there is no motion library in this repo); the card that leaves is a blank silhouette, so nothing a reader or a test can reach is on screen twice — it leaves FROM the point the hand released and in the verdict's own colour, and past the threshold the live card carries a ring in that colour, so which direction means what is learned while the finger is still down. The moving box is keyed on the card and the keyboard surface around it is not: replacing the card cannot carry a reader's tab stop away with it, and the transform a swipe left behind cannot spring back on the card that has just arrived. Emptied, it shows the cleared plate: the count decided and the time, from the deck's own memory of what it watched leave. `title` puts the deck's heading on the toggle's own row through `SectionHeader`, because a title above the deck and a control inside it are two rows saying one thing** | `decisiondeck.tsx` | ✅ |
| `RoleBadge` / `FieldGuard` | A principal's role, and a withheld value that reads as withheld rather than absent | `rbac.tsx` | ✅ |
| `ExplainNumber` | A converted aggregate opening into its contributing rows (FX lineage) | `explain.tsx` | ✅ |
| `MarginceCoreScene` | The product's one piece of AI identity: a glass ball with four ribbons turning inside it, drawn on the GPU, in the closed five-state vocabulary of the agent's work lifecycle (idle · ingest · working · warning · error). State is MOTION first — one behaviour each, from the table in `margince-core-motion.ts` — and colour second; only the two that stop leave the indigo. `aria-hidden`, and it carries no click. Callers pass `state` and never restyle: `size` picks the hero dress or the chrome one, `surface` says whether it is lit against something dark or opaque on paper, and sizing goes through `--coreSize` / `--coreGlass` / `--coreHalo`. A host without WebGL2 (jsdom included) gets the static dress, same `data-core-state`, no motion. `margince-core-shader.ts` (the GLSL) / `margince-core-gl.ts` (context and uniforms) / `margince-core-engine.ts` (the loop) are its internals, not a caller's API | `margince-core.tsx` | ✅ |
| `MarginceWorkbench` | The in-app agent workbench: steps, runtime chip, the Core in context | `margince-workbench.tsx` | ✅ (`Design System/Workbench`) |
| `RelationshipMap` | The account's routes drawn as a fixed three-column picture — our colleagues on the left, the account and its deal in the centre, their people in role lanes on the right. Data-only and copy-free: every string arrives translated, so it renders in a story with no locale. Selecting a person lights the STRONGEST route into them end to end and fades the rest, and the panel says in words which route was chosen and lists the ones that were not, so a reader can disagree with the pick. No physics — a lane's own order is the drawn order, so the same account draws the same picture on every read and a reader who has learnt where to look does not have to learn again. Every fact survives without colour: strength is stroke WIDTH and the weakest band is dashed as well, kind is shape, engagement is a word on the node. One tab stop for the whole map with the arrow keys walking it lane by lane; edges are `aria-hidden` because the same facts reach a reader through the node names and the panel | `relationshipmap.tsx` (geometry and route arithmetic in `relationshipmap.layout.ts`) | ✅ |
| `PipelineBoard` / `DealCard` | The pipeline surface and its cards. A card's company slot has THREE readings and the caller declares which: `org` names it, `orgWithheld` says the payload refused it, and an empty `org` with no flag is the one reading that draws nothing. The refusal is drawn HERE, as `FieldGuard`'s mask — the same control the deals table's company cell draws — because a caller handing in its own words for it is how one reading ends up with two spellings, and a monogram cut from the word "withheld" would be a mark no company has | `composed.tsx` | ✅ (`RecordView → BoardInSurface`, `BoardWithWithheldCompany`) |
| **`PageZones`** | **The page's columns, and the ONE grid that draws them: a work column plus up to two rails, in four named shapes (`single` / `rail` / `aside` / `both`). The work column always takes the largest share, folds to full width at 1200px while staying FIRST (a reader wants what is happening, not the firmographics), and the rails stack under it in one column at 720px. It carries the grid and nothing else — no name, no mark, no badge, no tabs — which is what lets a page that is not a record use it; `RecordView` composes it rather than owning it. `shape` names the TEMPLATE and the slots carry the content, because a grid track does not collapse when its item is missing: passing `both` with one rail draws an empty column** | `pagezones.tsx` | ✅ |
| `RecordView` | The record page shell: identity, readings, timeline — the columns underneath it are `PageZones`. `tabs` is the bar that chooses which part of the record is drawn under it, and it goes in the WORK COLUMN rather than the band: what it chooses is what appears beneath it, and in the band it would sit over the rail as well and claim to govern a column it does not. The interval under it and the one-row-that-scrolls behaviour are the slot's, spelled once — the person page and the lead page had written the same wrapper under two names before it existed | `composed.tsx` | ✅ |
| `.record-stack` | The work column's own stack: ONE vertical column of full-width panels, which is what a record page's body is. Not a grid of half-width cards — every panel in it is read top to bottom once rather than compared with the one beside it, and a panel whose body is prose gets a measure too narrow to read in half a column. A class rather than a component, because it is what a caller puts around its OWN panels; reach for it instead of a flex column of your own, which is how one record page came to space its cards differently from the next | `composed.css` | via `RecordView` |
| `GroupedTimelineList` / `TimelineRow` | The activity timeline, grouped | `composed.tsx` | via `RecordView` |
| `useRecordTimeline` / `useTimelineFilters` | The activity timeline's query and its filter state, so a record page does not re-derive either. `timelineQueryParams` builds the request, `hasTimelineFilters` answers whether the reader has narrowed anything, and `dayStartIso` resolves a day boundary in the workspace zone rather than the browser's | `recordtimeline.ts` | — |
| **`BriefItemCard`** | **One Morning-Brief queue entry: rank, the deal it is about, the §10.1 composite, the five-factor decomposition, the evidence behind it, the rep's own state, and the three verbs that move it — act / snooze / dismiss. Presentational and controlled: it fetches nothing, writes nothing, and every word arrives through `labels`. The two things that need a locale — a percentage and an instant — arrive as FORMATTERS rather than as rendered strings, which is what stops the headline figure and the bars from disagreeing about what 0.62 is called. The bars share ONE grid and the composite is its first row: five tracks that each size themselves are five readings, five that start and end at the same x are the comparison a decomposition exists to be. A settled item RECEDES rather than dims — `Card`'s own `inset` variant — because the blanket opacity this replaced dimmed the state word and the focus ring along with everything else. `snooze` is first-class: the API has served it since before any UI offered it, and a snoozed item keeps its verbs and says when it comes back, because an item deferred to nowhere in particular is indistinguishable from one quietly dropped. `BriefItemCardPending` is the card-shaped wait. Two surfaces draw it — Home and the Worklist — and neither wires it directly: `screens/briefqueue.tsx` holds the labels, the two formatters, the per-item pending and error projection and the three mutations, so the pair cannot come to disagree about what a verb does or when a set-aside item returns. It is CONNECTED and therefore stays in `screens/`, not here.** | `briefitem.tsx` | ✅ |
| `ProviderMark` | A federated provider's own sign-in mark — the ONE file allowed literal colours, because another company's colours are not ours to tokenise. It leads a `Button variant="federated"` and keeps its own size there: `.btn svg` sizes a lucide glyph to the label beside it, which is right for a glyph and wrong for a mark drawn to its own proportions | `provider-mark.tsx` | via `Atoms → Buttons` |
| **`SurfaceState`** | **The nine-state honesty vocabulary — `ready \| empty \| withheld \| unavailable \| loading \| unsupported \| failed \| stale \| partial` — as ONE component, with `sectionState()` to classify a composite read's section and `omitted()` to ask whether a grant withheld it. `empty` is the only state allowed to say "there is none", because it is the only one that knows; drawing any of the other eight as empty states a fact the page does not have. Two ordering decisions are load-bearing: `stale` puts its caveat ABOVE the rows (a caveat under a figure arrives after the reader has taken it as current) and `partial` puts its count BELOW them. A `failed` with no `onRetry` is `unavailable` with extra words. The nine sentences are keyed `state.*`; what there is none OF stays the caller's word, in `emptyLabel`, and what is being WAITED for is `loadingLabel` — the loading arm used to be a mute 32px bar, and three screens had bolted their own visually-hidden line beside it rather than being able to hand it one. `label` names one part of a multi-part card, and `labelLevel` says how deep it sits: `h3` by default, `h4` for a card that is itself a section of another, so the outline nests instead of flattening into a row of equal siblings. `detail.withheldReason` replaces the generic withheld sentence with the payload's own: "you cannot see this" is true of every withheld section and tells a reader nothing about what is missing from the one in front of them, and a response that names the source (the meeting brief's `omitted[]`) spends it here** | `surfacestate.tsx` | ✅ |
| **`Eyebrow`** | **The one spelling of uppercase micro-type: 11px, `--fw-semibold`, `--tracking-eyebrow`, `--textMeta`. `as` picks `h2` / `h3` / `h4` / `span` / `dt`, because the same look is a real heading over a section and a plain label beside a value, and nothing about the type says which. The declarations live in `base.css` as `.t-eyebrow` so a selector-only site (`.firmo dt`) can reach them too** | `eyebrow.tsx` | ✅ |
| **`IconAction`** | **A verb whose glyph is its whole label: square, named through `aria-label`, and named again on hover through `useTooltip` — ONE `label` prop feeding both, so the name a screen reader hears and the name a pointer reveals cannot come apart. `Button iconOnly` draws the square and states that the caller owes it an accessible name; what it cannot do is give a SIGHTED reader that name, and a row of unlabelled glyphs is a row of guesses. Reach for it only where the glyph already IS the verb — mail, phone, calendar, pencil, link, the overflow ellipsis. A verb whose consequence must be read before it is pressed (convert, merge, archive, disqualify) keeps its words, and a verb rare enough to need explaining belongs in an `OverflowMenu` where it can have a whole line: squaring those two saves a few pixels and costs the reader the one thing they needed. `reason` / `reasonId` and `pending` pass straight to `Button`, so a refused or mid-write square behaves exactly as its labelled sibling does. `pressed` is for a glyph that SETS rather than does — a switch over a region, a filter left on — and draws `aria-pressed`, because two states of one switch look identical and a glyph has no label to carry the difference; it is omitted on a verb, where "pressed" would claim a state the control has not got** | `iconaction.tsx` | ✅ |
| **`useTooltip`** | **A control's own name on hover and on focus, for a control that cannot draw it. Same machinery as `useTruncationTooltip` beside it — they differ only in WHEN a tip is worth showing (always, versus only for a string the row clipped) and in whether the anchor earns a tab stop, which a button already has. It describes rather than names: the control owns its name already, so the tip lands in `aria-describedby` and the control does not announce itself twice** | `tooltip.tsx` | via `IconAction` |
| `Breadcrumb` | The trail that says where the reader is and how they got here: a `nav` landmark the caller names, an `<ol>`, one `<li>` per stop. It refuses the three things a hand-rolled trail gets wrong. The LAST stop is the current page and is never a link, even when the caller passes it an `href` — a link to the page you are already on is a control that does nothing. Separators are `aria-hidden` spans INSIDE the item they follow, never list items of their own (a three-stop trail would then announce as five things) and never CSS `content` (some screen readers speak it and some do not, so the same page reads differently to two people). And only the last stop gives way under pressure: ancestors are short labels the product chose, and shortening them would hide the way back, while the last is user data of unbounded length — `useTruncationTooltip` carries the rest of it on hover AND on focus. Takes no `className` or `style`: a trail restyled per screen is how one product comes to draw two different crumbs | `breadcrumb.tsx` | ✅ |
| **`CardBoundary`** | **A render boundary around ONE card. The app-level boundary is the floor, not the story: by the time it catches, the whole shell — navigation rail included — has unmounted, so one card's throw costs the reader every way out. This one keeps its place, says the card failed, and retries with the query cache reset beside its own state. It never shows the thrown error's text: a render throw names our internals and a reader cannot act on it** | `cardboundary.tsx` | ✅ |
| **`PipelineLadder`** | **One message's path through the ingress pipeline, top to bottom, with what each step did and why. It holds NO list of stages — it walks whatever the server sends, so a step added later appears with no frontend release. An unrecognised stage renders from the server's own `label` / `reason_text` rather than vanishing or printing a raw key: a closed set is right for a five-value enum and wrong for a vocabulary designed to grow, where an omission is indistinguishable from "nothing happened". The nine statuses keep four distinctions apart that a reader would otherwise conflate — `not_applicable` (did not apply) vs `unknown` (swept, we cannot tell), and `withheld` (rendered UNCONDITIONALLY to a non-owner, so its presence is not a row-existence oracle) vs an omitted rung** | `pipelineladder.tsx` | ✅ |
| `ListTable` | A record list as a table: columns, rows, the query dials above them, and the footer under them. Controlled — the screen owns the sort/filter/search state the server answers to. `selection` adds row checkboxes (inside the frozen identity cell) and a bulk bar above the grid; the screen owns the verbs and their per-row writes. A column declares its KIND and the table sizes it: `fixed` is the identity column, `numeric` right-aligns an amount, and `verbs` says the column holds buttons rather than a value — sized by the buttons, in pixels, since a translated label is not a fraction of a page. Every other column takes a share of what is left and stops at its own minimum, at which point the BODY scrolls sideways (keyboard-reachable and named, via `TableScroll`'s hook) rather than the columns being crushed: a settings column is 654px wide and a five-column table is not. The rendered PAGE is the table's own by default and the caller's when it passes `page` with `onPage` — the address is where a list screen keeps it, so a reader who paged through a list and opened a record comes back to the page they left. Narrowing still resets it to one, compared by VALUE rather than by counting effect runs: an effect runs on arrival, twice on arrival under StrictMode, and again for any dial that settles a tick after mount, and a reset that believed those threw the reader off the page their own address had asked for. `bodyRef` hands back the element the rows scroll in, for a caller that remembers where the reader was (`app/scrollmemory.ts`) — a full-height list is the one place the page column never moves, so the rows are their own scroll lane | `listtable.tsx` | ✅ |
| `ListSurface` | The chrome `ListTable` renders into, usable on its own for a list that is not a table: saved-view tabs, the count line, the search field, filter chips, the sort menu, the archived toggle, and the footer. The toolbar reads as two halves — what NARROWS the list on the left, what changes how it is DRAWN on the right — so a reader looking to cut the set down never scans the whole row to find out which controls do that | `listsurface.tsx` | via `ListTable`, and directly in `RecordView → BoardInSurface` |
| `Menu` | The popover panel `ListSurface` hangs its sort, filter and column sets in — a `fieldset` with a heading, because a set of controls that belong together IS one, and `align` picks which edge it opens from. Reached through the surface's own dials; there is no second spelling of a dropdown panel | `listsurface.tsx` | via `ListTable` |
| **The sort menu** | **Every attribute the server can order the list by, in one place — because a column header is the other route to that state and cannot be the only one. A header reaches only the columns currently SHOWN, at a width a phone does not have, and a sort restored by a saved view arrives with no header pressed at all. `ListTable` derives the menu from its own columns, so a column naming a server sort field is offered here whether or not it is on screen, and hiding a column is not giving up ordering by it. It also offers the server's OWN order, which a saved view naming no sort asks for and nothing else could ask for by hand. Both ends read `nextSortValue`, so the header and the menu cannot disagree about which press descends** | `listsurface.tsx` | ✅ (`ListTable → SortedByAMenu`) |
| `CountLine` | The one sentence under a list saying what is on screen out of what exists, and what it is sorted by. It is where "23 of 1,204" is spelled, so a screen never invents its own phrasing for the same fact — and `more` is what keeps an unknown total from being reported as a known one | `listsurface.tsx` | via `ListTable` |
| `InlineChoice` / `InlineText` | Hover-to-edit ON a record page: a chooser that commits the instant something is picked, and a text field that commits on Enter or blur. Both show the plain VALUE with no hover affordance to a reader who may not edit it, keep the typed answer when a save is refused, and treat re-picking the stored value as no edit at all. `InlineText` takes `multiline` for a value that is a PARAGRAPH rather than a line — the account's own story fields — where Enter inserts a newline and the commit key becomes Cmd/Ctrl+Enter, blur committing exactly as it does for a line. `FieldRow` is what puts them in the grid | `inlinechoice.tsx` | via `FieldGrid` |
| `Logomark` | The product's own "M". One mark, every fill on `currentColor`, so the shell chip and the onboarding speaker cannot drift into two | `logomark.tsx` | ✅ |
| `useTruncationTooltip` | The whole of a string its row had to truncate, on hover or focus — and nothing at all when the string already fits. Reach for it wherever user data of unbounded length is drawn on one line: a record name, a trail segment, a rail row's reading | `tooltip.tsx` | ✅ |
| `usePrefersReducedMotion` / `useTypeStream` / `useDocumentIntro` | Motion, with one rule: reduced motion jumps to the END state, never to nothing | `motion.ts` | — |
| **`.arrive` / `.arrive-stack`** | **The arrival: content fades over `--dur-enter` and rises over `--dur-move`, staggered `--stagger-enter` apart, four deep and then together. A container whose CHILDREN arrive says `.arrive-stack` and its children need no class — a screen should not have to remember to label every card. `.wrap`, the shell's page gutter, is named in the rule itself, so every screen gets it without a call site. Two things to know before using it. A container that is a stack does NOT itself arrive (two nested fades make a dimmer fade, not a longer one), so mark every link of the chain where a screen keeps its blocks a level deeper. And it plays when an element is INSERTED, which is why a tab strip whose panel is a fresh element gets its transition for free while the strip itself stays still — that is the whole tab-panel animation, with no wrapper and no state** | `enter.css` | via every screen |
| **`--focus-ring` / `--focus-glow` / `--focus-ring-forced`** | **The focus ring, in the three shapes this product has and no more. An OUTLINE for a control sitting on a surface — outside the box, so it never changes the control's size. A GLOW for a field, because a field already has a boundary and an outline around a bordered box reads as two boxes. And a transparent outline PAIRED with the glow for a control that is neither, which replaces the platform's off-palette ring and holds the outline property at a real width so forced-colors mode still has something to paint. Ten spellings preceded them, two of which drew nothing at all. The offset stays per-component: a control clipped by its own container draws the ring inside itself or loses half of it. A fitness function refuses an eleventh spelling** | `tokens.css` | — |
| **`useUnsavedGuard` / `UnsavedGuard`** | **Unsaved edits, and what happens when the reader leaves without saving them — where the answer used to be nothing. `useUnsavedGuard(dirty)` is called by whatever holds a draft, from wherever it is; `UnsavedGuard` wraps the surface and holds ITS OWN content until the question is answered, so the draft is still on screen while the reader decides. Two mechanisms because there are two ways to leave: a reload is the browser's to ask about through `beforeunload` (registered only while dirty — a listener that is merely present costs the page its back/forward cache), and a move within the app gets this product's `ConfirmModal`. Escape and the backdrop KEEP the edit: a dialog whose dismissal destroys work punishes the reader for not reading it. Scoped to the surface rather than wired into `hashchange` on purpose — a router-level guard also intercepts Back and Forward, and getting that wrong breaks navigation for everybody, which is worse than the loss it prevents** | `app/unsaved.tsx` | — |
| **`useUrlParams`** | **The query half of the address, and the ONE way to read or change it: how a list is narrowed, in what order, on which page. The path names WHAT is on screen (`app/router.tsx`); this names which VIEW of it, which is why `routeIdentity` ignores it and a dial change re-renders instead of throwing the screen away. Writes REPLACE the current history entry — a reader turns several dials to reach one view, and pushing each one would bury the screen they came from so deep that Back stops being a way out — so the entry left behind always describes the list as it stands, which is exactly what makes Back from a record restore it. A `ReadonlyMap`, not `URLSearchParams`: one value per dial, and a snapshot that cannot change underneath, which is what lets a screen DERIVE its query from the address rather than mirror it into state that then disagrees with the URL bar. `currentParams()` is the same thing outside a render, for two writes in one handler that must compose. The list-specific half — which dial goes by which name — is `listQueryFromParams` / `paramsFromListQuery` in `screens/listquery.tsx`, bound once inside `useListQuery`, so a list screen is addressable without spelling anything itself. A screen with a DRAWING dial of its own — board or table, which pipeline's board — names it in `screenDials` and the codec holds it out of the wire filters; unheld it was spread onto the request as a filter, counted as a narrowing and wiped by "clear filters". The deals board composes the codec directly rather than through the hook (it draws two surfaces from one query), so it passes the same names to `withoutScreenDials` / `mergeScreenDials` itself** | `app/urlstate.ts` | — |
| `subscribeToWindowFocus` and friends | Whether this window has focus — one signal for the draw loop and the stylesheet alike | `window-focus.ts` | — |

Tokens and sheets, none of which a caller redeclares: `tokens.css` (the Ledger
Green canon, pinned by `tokens.test.ts` — the only file where a literal colour
may appear), `brand.css` (the derived layer: `color-mix()` over a canonical
token, never a new hex), `base.css`, and the per-component sheets a component
imports itself. `interaction.stories.tsx` catalogues the colours the *browser*
owns — caret, checkbox tick, scrollbar thumb, selection — which are set once at
the document root and belong to no component.

## Building a list screen

**A record list is not a table you write. It is `useListQuery` bound to
`ListTable`, and the whole of a screen's job is declaring what its records are.**
`screens/listquery.tsx` holds the binding; `screens/products.tsx` is the
shortest complete example, and `ListTable → Default` / `Paged` /
`SortedByAMenu` in `listtable.stories.tsx` are the surface on its own.

Four dials, and each one is the SERVER's answer rather than a slice of what is
already on screen. A table that quietly reordered its own page would be lying
about the other pages, and this list is a keyset cursor over a set larger than
what is loaded.

| Dial | Where it lives | The rule |
|---|---|---|
| Search | `q`, debounced by `SEARCH_DEBOUNCE_MS` | `searchable={false}` for a list whose GET has no `q`, and then no box is drawn. A box that narrowed nothing is worse than no box |
| Sort | `sort`, one field plus the house tie-breaker | A column declares the server field it orders by; that is what makes its header live and puts it in the sort menu. **A screen may not name an ordering its endpoint cannot give** — held by `screens/list-sort-claim-coverage.test.ts` |
| Filters | one wire param per chip | A chip's key IS the parameter name, so the address and the endpoint describe a narrowed list in one vocabulary. `dataChips` for options the server named at runtime |
| Page | `page` + `per` in the address, `cursor` on the wire | The reader's place survives opening a record and pressing Back. A read fetches a whole multiple of the rendered page size through `listFetchLimit`, so the pager can offer numbers that need no round trip — held by `screens/list-page-size-coverage.test.ts` |

What that buys, and what a hand-rolled list gives up: every dial addressable, so
a narrowed list is a link somebody can paste; Back from a record returning to the
page and scroll position it was opened from; one `CountLine` phrasing for what is
on screen out of what exists, which says "loaded so far" rather than inventing a
total a keyset cursor cannot know; the reader put back on page one when a
narrowing changes what page one means, and NOT when they merely arrived.

Three things to get right at the call site:

- **A dial the endpoint does not answer is not drawn.** Not disabled, not
  present-but-inert — absent. `/partners` advertises a `sort` its handler never
  reads, and the screen that trusted it opened on a tab labelled "Newest" while
  drawing rows in uuid order. A wrong order looks like data; a missing control
  looks like a missing control.
- **Two lists on ONE route need `paramScope`.** They share an address otherwise,
  and one flat parameter space describes both at once — the products table sent
  its `sort=sku` to `/offer-templates`, whose vocabulary has no such field. Lists
  that own their address alone keep bare names, because `#/companies?q=acme` is a
  link people paste.
- **A drawing dial of the screen's own is named in `screenDials`.** Board or
  table, which pipeline's board: it belongs in the address and not on the wire.
  Unheld, `view=board` was spread onto the request as a filter, counted as a
  narrowing, and wiped by "clear filters" — pressing which flipped the board back
  to a table.

`emptyNote` is the sentence a screen adds when it knows why its list is empty,
and it is drawn under the narrowed line as well as the bare one, because the case
it exists for is narrowed: a "Mine" tab for a reader who owns nothing. Which
emptiness a note explains is the CALLER's to know — `mineEmptyNote` in
`screens/recordlist.tsx` is that sentence for the three owner-scoped lists, and a
caller whose note would blame the data source for what the reader's own dial did
passes none.

## Absent, disabled, or withheld — decided by CAUSE

A surface a reader cannot use is in one of three states, and **which one is not a
style choice — the cause picks it.** Getting this wrong is not a cosmetic bug: an
absent card and an empty card make the same shape on screen and mean opposite
things.

| Cause | State | What the reader gets |
|---|---|---|
| It does not apply here — a posture, a rollout flag, a capability this installation does not have | **absent** | Nothing. There is no fact to report. |
| A precondition the reader could fix is unmet — nothing selected yet, a write in flight, delivery not configured | **disabled** | The control, inert, **and what would make it live**. |
| A **permission** denies it | **withheld** | The surface keeps its place and **says that it is withheld**. |

The third row is the one that gets broken, because returning `null` on a denial
is the shortest code and looks tidy in review. It is a false statement. A
retention card that vanishes for an ops seat does not read as "not yours" — it
reads as "this installation keeps nothing", and an absent audit trail reads as
"nothing has happened here". Both are claims about the DATA, made by accident,
in place of a claim about authority.

Three consequences worth stating, because each was a real defect:

- **A withheld card asks the server for nothing.** The answer is already known,
  so keep `enabled: canRead` on the query. Withholding is about what the page
  SAYS, not about issuing a request in order to be refused.
- **Gate on the probe, not on its absence.** `/me` in flight is not a denial;
  branching before it answers flashes the notice at every reader.
- **A write affordance inside an otherwise readable surface may be absent**,
  provided the surface states its read-only posture once (`auto.readOnly`,
  `cf.noPermission` are the pattern). Withholding twelve buttons individually is
  noise; withholding the page's one explanation is the defect.
- **A surface that is only an ACTION may be absent on a denial.** The rule above
  is about not making a false claim, and a card holding no fact cannot make one —
  there is nothing for a reader to misread as "zero" or "nothing happened". The
  danger zone is the case: an absent Reset-data card says nothing about the
  installation, while "you may not reset this installation" is noise on every
  page that renders it. A surface that reports anything at all is not this.

**`SurfaceState` is the primitive for the third row**, and for the six other
things a surface can be that are not content. Reach for it before hand-rolling
a message line: it already knows that withheld, unavailable and unsupported are
three different sentences, and that only `empty` may claim there is none.

Three more things carry this properly and are worth copying. `Switch`'s
`reason` prop renders the explanation, points the control at it with
`aria-describedby`, **and refuses the flip by itself** — the caller does not
owe it a `disabled` beside it and cannot cancel it with one, because a sentence
saying a setting is not yours to change, over a switch that changes it, is a
denial the reader disproves with one click. `Button`'s `reason` does the same for an ACTION — it
disables the button, renders the sentence beside it and wires
`aria-describedby` — because a `title` on a disabled button is announced by no
screen reader and a disabled button cannot be focused, so a reason living only
in `title` reaches exactly nobody who needed it; `Button`'s `reasonId` points
several refused controls at ONE sentence already on the page, for a surface
where one fact refuses all of them. And `FieldGuard` covers a withheld VALUE
rather than a withheld surface. What is left hand-rolls
`<EmptyState><p className="t-small">{t(…)}</p></EmptyState>` as the card body,
which is the shape to match where `SurfaceState` does not fit.

`Switch` versus `Checkbox` follows from the same honesty: a `Checkbox` states an
intent that something later submits, a `Switch` **is** the action. A control that
writes when you flip it and announces itself as a checkbox has told the reader
the wrong thing about what their next click does.

That pairing is also the answer for a **stateful** control a permission denies —
one that is the only place a reader can see the setting's current value. Absent
would hide a granted read; withholding the surface would hide the fact. A
`Switch` carrying `reason` shows the state, refuses the change, and says
why, with the explanation attached to the control rather than sitting beside it.

## Seeing them

```sh
cd frontend && pnpm storybook      # the catalog on :6006, light and dark
```

The Theme control in the toolbar flips `data-theme` exactly the way the shell
does, so every token re-resolves — check both before you call a surface done.
Stories live beside their component as `<name>.stories.tsx`; that co-location is
also what the change-scoped capture gate keys on
(`frontend/scripts/fe-uat.mjs`).

The sidebar has eight roots, and a story's `title` is the only thing that puts
it under one — the gate keys on `importPath`, never on the title, so a title is
free to describe the surface rather than the file:

| Root | What is under it |
|---|---|
| `Design System/` | One node per primitive in this directory. |
| `Records/` | The screens a rep works in, and the cards on them (`Company 360/`, `Company rail/`). |
| `Settings/` | `You/` and `Admin settings/`, mirroring `SETTINGS_TABS` in `screens/settings.tsx` one for one — group label included — with the card as the leaf. |
| `Patterns/` | Screen-tier building blocks that are not a page: the query gate, the create/edit/merge/share actions, the composer. |
| `Onboarding/`, `Signed out/`, `Shell/` | The first run, the pages reachable without a session, and the application frame. |
| `MCP Apps/` | The governed tool surfaces and their document forms. |

Leaves are Sentence case throughout. The trap the old flat `Screens/` root left
behind is worth naming once: `Company connections` was the company rail's
relationship graph, not the settings Connections tab, and the tab it looked like
had no story at all. It is `Records/Company rail/Relationship graph` now.

## Driving a control in a test

`Select` is a button and a portalled listbox, so `userEvent.selectOptions` does
not apply to it. Use the one helper:

```ts
import { pickOption } from "../design-system/select-testing";

await pickOption(user, screen.getByRole("combobox", { name: "Stage" }), "Won");
```

## The gates that enforce this

All of them run in `make frontend-check`. Most are script gates that act as a
fail-closed grep arm on top of the vitest suites — the discipline holds even if
the test tree regresses. `native-controls.test.ts` is the exception: a
TypeScript-AST vitest gate rather than a shell script, because a parser already
knows how to tell a real element from one inside a comment or a string, which
the shell script it replaced could not.

| Gate | What it refuses |
|---|---|
| `make native-controls` (`src/design-system/native-controls.test.ts`) | `<select>` / `<option>` / `<optgroup>` anywhere under `src/`, and in every extension frontend layer — with **no exemption**, `design-system/select.tsx` included: it contains no native control, so the old shell gate's path exemption covered nothing |
| `frontend/scripts/check-ds-purity.sh` | A hex literal or `rgb()`/`hsl()`/`oklch()` outside `tokens.css` |
| `frontend/scripts/check-font-lock.sh` | A fourth type family |
| `frontend/scripts/check-icon-glyph.sh` | An emoji glyph in a source string — Lucide only |
| `frontend/scripts/check-ds-spacing.sh` | New raw-px margin/padding/gap outside this tier (diff-scoped) |
| `design-system/conformance.test.ts` | Hard-coded user-facing copy, the same colour and font rules, one stylesheet per class namespace; a reduced-motion rule a later plain rule of equal specificity beats; an infinite animation with no reduced-motion answer; a second home for the pending announcement or the placeholder pulse; a focus ring that spells its own width and colour |
| `design-system/tokens.test.ts` | A token whose value drifted from the design canon |
| `design-system/catalog.test.ts` | A component in this directory that this table never names; a row claiming a story of its own where no story file exists; a story anywhere under `src/` filed under a root the sidebar table above does not document |
| `e2e/` (axe) | WCAG 2.2 AA on every core screen, plus the 390px no-horizontal-scroll sweep |

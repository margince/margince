/bin/
*.test
coverage.out
# The extension units' merged profile (make test-extensions EXT_COVER_OUT=…).
# Same debris as coverage.out and it was never listed: only CI wrote it until
# main-health's backend gate started writing it too, and now anyone reproducing
# that gate locally has it in `git status`.
coverage-extensions.out
.env
.build/
.DS_Store

# ADR-0069 stable extension tier: build/composition/ is the ONE ignored
# root for installation-dependent generated output (digest-gated by its
# composition.json, never committed — anchored to the composition root so
# an unrelated future /build asset is never silently hidden), and
# extensions/ is the installation-owned enabled set. The vanilla tree
# ships the placeholder, the approval lock, and the FIRST-PARTY units
# enabled by default (extensions/de, the German jurisdiction pack;
# extensions/openchannel, the reference connector — presence under
# extensions/ IS the enablement, so a unit missing from the un-ignore
# list below is a unit no clone of this repository has).
/build/composition/
# The SECOND composition root, and it exists for a reason that is not tidiness:
# gen-composition's -verify requires build/composition/ to hold EXACTLY the files
# it generated, so an openapi-typescript output written in there is refused as an
# injected extra file on the next `make check-composition`. The merged-contract
# TS types therefore get their own root. Same rule as its sibling — generated by
# `make fe-typecheck-composed`, never committed.
/build/composition-frontend/

# The macOS desktop bundle's build output (`desktop/build/*.sh`): a staged
# Postgres+pgvector tree, the compiled binaries, and the assembled
# distributable folder. Anchored like build/composition/ above so an
# unrelated future /build asset is never silently hidden.
/build/desktop/

/extensions/*
!/extensions/.gitkeep
!/extensions/approvals.lock
!/extensions/de
!/extensions/openchannel
# A unit's installed frontend dependencies. Its package.json and the workspace
# lockfile are what ship; node_modules is what pnpm makes from them, and it is
# per-member now that a unit's frontend layer is a workspace package.
/extensions/*/frontend/node_modules/

# The desktop launcher compiled in place: `go build ./...` in its module dir
# names the output after the directory, so it lands beside its own sources.
# The real build (desktop/build/build-app.sh, build-app.ps1) writes to
# build/desktop/.
/desktop/launcher/launcher
/desktop/launcher/launcher.exe

# Compiled cmd binaries left in the module dir by `go build ./cmd/<role>`
# (anchored so they never match the cmd/<role> source directories; the api
# binary can't land here — backend/api/ is the OpenAPI contract directory).
/backend/worker
/backend/migrate
/backend/mcp

# Same for the tools module, and BOTH paths it lands on: `go build ./tools/...`
# from backend/ drops the binary at the first, `go build ./...` from
# backend/tools/ drops it beside its own source at the second. A `git add -A`
# will happily commit either — it has done both, the second time because only
# the first was anchored here. An anchored rule is right for a build artifact
# whose name collides with a package directory, so the two are listed rather
# than loosened to a bare `seed-demo`.
/backend/seed-demo
/backend/tools/seed-demo/seed-demo

# Session working notes — local only, not part of the published repo
/review_*.md

# Superpowers skill artifacts (design/plan/spec docs) — local working notes,
# kept on disk to guide the work but never committed to the repo.
/docs/superpowers/

# The company-record-page V2 implementation handoff and its state mockups.
# A working handoff for whoever picks the slices up, not a repo doc; the tracked
# record is the GitHub issue for that work.
/docs/explanation/company-record-page-v2-implementation-plan.md
/docs/explanation/assets/company-record-page-v2/

# Local secrets — never commit. Globbed rather than the bare name: a hand or
# editor backup (.env.local.bak) is the same secrets file under a name the
# exact rule never matched, and one was committed that way — where it then went
# stale as a second copy of .env.example.
.env.local*

# A local routing file, if you keep one. Nothing seeds it any more — the
# installation's binding is a stored setting, declared for a fresh stack under
# `seeds.ai_routing` — but the debug and certification lanes still take a file,
# and a hand-written one belongs to its author. Anchored so the committed
# .example templates are never matched.
/config/ai-routing.yaml

# Per-engineer dev deployment config — `make dev` seeds config/margince.yaml
# from config/margince.example.yaml on first run and then leaves it (edit it to
# change org/admin or AI posture). The admin password file it references is
# seeded alongside. Anchored so the committed .example template is never matched.
/config/margince.yaml
/config/margince-admin-password
# The entitlement token license.token_file points at, if an engineer keeps one
# here: a real license is a credential, and this is a public repository.
/config/margince-license

# Storybook static build + fe-uat capture artifacts
frontend/storybook-static/
.tmp/

# Generated SBOMs + cosign signatures (build artifacts — `make sbom`, published by CI)
/sboms/

# Factory boundary backstop: a dispatched worker that mistakenly writes
# factory artifacts (plans/reports/UAT guides) into the worktree can never
# stage them (target-minimum-setup §1).
workspace/

# Playwright MCP scratch: page snapshots, console logs and screenshots a
# browser-driving session writes as it works. Debris from testing, never
# product — and it lands here by default, so it is ignored by name.
.playwright-mcp/

# Agent debris that has slipped into the repo before — never product:
#   - the pnpm content-addressable store (machine-local build cache)
#   - a repo-root scratch dir (real scratch belongs in the session temp dir)
#   - Codex visualization exports (conversation artifacts, not product source)
#   - agent worktrees. A dispatched agent working on an isolated copy of the repo
#     puts it here; it is a second checkout, not product, and staging it would
#     commit the whole tree twice.
#   - screenshots dropped at the repo root. Root-ANCHORED so tracked product
#     assets like frontend/src/assets/*.png are unaffected.
#   - screen RECORDINGS, by the same rule and for the same reason. Proving an
#     MCP App view renders is a video job, so this branch is the one that
#     starts producing them — the line belongs with the change that creates
#     the risk rather than in a tidy-up nobody files. These are NOT anchored,
#     unlike the images above: the images are anchored because the product
#     tracks some (frontend/src/assets/*.png), and it tracks no video at all —
#     so a recording dropped beside the view it was recording is exactly the
#     case worth catching.
/.pnpm-store/
/node_modules/
/scratchpad/
/.codex/visualizations/
/.claude/worktrees/
/*.png
/*.jpg
/*.jpeg
/*.gif
*.mp4
*.mov

# JetBrains project settings: per-developer IDE state (run configs, local
# inspection profiles), not a build input. Machine-local, so it never belongs
# in a commit.
/.idea/

# Agent-tooling scratch: state a Claude Code plugin keeps for its own runs,
# written into whichever tree it was invoked from. Machine-local and per-session,
# same as the IDE state above.
/.impeccable/
/config/margince-setup-token

# Compiled generator binaries. `go build` inside a tool directory drops one
# beside the source, and it is machine debris the same way a build cache is.
# The .exe half is Windows, where `go build` names the binary that way.
/backend/tools/gen-aitasks/gen-aitasks
/backend/tools/gen-aitasks/gen-aitasks.exe
/backend/tools/gen-jobs/gen-jobs
/backend/tools/gen-jobs/gen-jobs.exe
/backend/tools/gen-stubs/gen-stubs
/backend/tools/gen-stubs/gen-stubs.exe
/backend/tools/gen-agentpolicy/gen-agentpolicy
/backend/tools/gen-agentpolicy/gen-agentpolicy.exe
/backend/tools/gen-recordfields/gen-recordfields
/backend/tools/gen-recordfields/gen-recordfields.exe
/backend/tools/gen-perfdoc/gen-perfdoc
/backend/tools/gen-perfdoc/gen-perfdoc.exe
/backend/tools/contract-overlay/contract-overlay
/backend/tools/contract-overlay/contract-overlay.exe
/seed-demo

# e2e-llm run artifacts. The lane writes a transcript per run (hundreds of KB
# each) plus a small verdict file. They are evidence for the run that produced
# them, not repository content — read them locally, do not carry them in git.
e2e/llm/records/

# The three binaries `make dev` and the build targets produce. Named here for
# the reason every other compiled path above is: a build artifact committed
# once is a stale binary every later reader has to notice is stale.
/backend/bin/

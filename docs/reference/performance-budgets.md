<!-- GENERATED by `make bench-record` / `bench-capture` / `bench-mobile` — do not edit by hand. -->

# Performance budgets — published, and what was measured

Every performance budget this product publishes, and the last measurement taken
for it. Rendered by `backend/tools/gen-perfdoc` from the records under
`docs/reference/perfbench/`, which the benchmark targets write as they run.

**These numbers are a record, not a gate.** `docs/reference/rbac-matrix.md` is
derived from the seeded policy, so it is checked for drift — rendering it twice
must produce the same bytes. A latency is measured rather than derived, so it
never renders the same twice and no equality check can be held over it. What is
held instead: a measurement says what machine it ran on, and **a budget nobody
has measured is printed as unmeasured rather than left out**. The budgets are
the published claim; the records are only evidence about one machine on one day.

Re-measure with `make bench-record`, `make bench-capture` or
`make bench-mobile` (each needs `make db-up` except the last). None of
them runs in CI, so nothing here changes unless somebody chooses to measure.

## The budgets

| ID | Operation | Published budget | Measured p95 | Verdict | Measured on |
|---|---|---|---|---|---|
| `PERF-1` | Record open (person/org/deal) | < 100 ms server | 3.2 ms | within budget | 2026-08-15 |
| `PERF-2` | List/table view (50 rows, filtered) | < 150 ms server | — | **not measured** | — |
| `PERF-3` | Search (full-text) | < 200 ms | 76.5 ms | within budget | 2026-08-15 |
| `PERF-4` | Save/mutation | < 150 ms server | 6.1 ms | within budget | 2026-08-15 |
| `PERF-5` | AI baseline action (summary/draft) | first token < 1.5 s | — | **not measured** | — |
| `PERF-6` | Cold start (single binary) | < 2 s | — | **not measured** | — |
| `PERF-7` | Context-graph assembly | < 300 ms at mid-market | 9.9 ms | within budget | 2026-08-15 |
| `CAP-PARAM-1` | Capture to timeline | 60 s p95 | 39.2 ms | within budget | 2026-08-15 |
| `MOBILE-AC-2` | Record open, perceived, Fast-3G | < 300 ms perceived | 49.0 ms | within budget | 2026-08-21 |

### Still unmeasured

No harness covers `PERF-2` (List/table view (50 rows, filtered)), `PERF-5` (AI baseline action (summary/draft)), `PERF-6` (Cold start (single binary)). They are listed above as `not measured` rather than
omitted, because a budget missing from this page reads as one that holds.

## Machines

A latency is only true of the machine that produced it.

### `make bench-capture` · measured 2026-08-15

| | |
|---|---|
| os / arch | darwin / arm64 |
| cpu | Apple M4 (10 cores) |
| memory | 24 GiB |
| toolchain | go1.26.6 |
| postgres | 16.14 |

| measurement | p50 | p95 | p99 | budget | samples |
|---|---|---|---|---|---|
| `capture_to_timeline` | 27.8 ms | 39.2 ms | 42.9 ms | 60000 ms | 20 |

### `make bench-mobile` · measured 2026-08-21

| | |
|---|---|
| os / arch | darwin / arm64 |
| cpu | Apple M4 Pro (12 cores) |
| memory | 24 GiB |
| toolchain | node 24.16.0 |
| network | throttled Fast-3G (MOBILE-PARAM-2) |
| viewport | 390x844 |

| measurement | p50 | p95 | p99 | budget | samples |
|---|---|---|---|---|---|
| `record_open_perceived` | 45.0 ms | 49.0 ms | 843 ms | 300 ms | 20 |

### `make bench-perf` · measured 2026-08-15

| | |
|---|---|
| os / arch | darwin / arm64 |
| cpu | Apple M4 (10 cores) |
| memory | 24 GiB |
| toolchain | go1.26.6 |
| postgres | 16.14 |

| measurement | p50 | p95 | p99 | budget | samples |
|---|---|---|---|---|---|
| `search_fts (mid_market tier)` | 72.7 ms | 76.5 ms | 81.4 ms | 200 ms | 20 |
| `context_graph (mid_market tier)` | 8.0 ms | 9.9 ms | 10.0 ms | 300 ms | 20 |

### `make bench-record` · measured 2026-08-15

| | |
|---|---|
| os / arch | darwin / arm64 |
| cpu | Apple M4 (10 cores) |
| memory | 24 GiB |
| toolchain | go1.26.6 |
| postgres | 16.14 |

| measurement | p50 | p95 | p99 | budget | samples |
|---|---|---|---|---|---|
| `record_open_person` | 2.9 ms | 3.2 ms | 3.4 ms | 100 ms | 30 |
| `record_open_organization` | 2.8 ms | 3.2 ms | 3.3 ms | 100 ms | 30 |
| `record_open_deal` | 2.3 ms | 2.5 ms | 2.7 ms | 100 ms | 30 |
| `record_save_person` | 5.6 ms | 6.1 ms | 6.4 ms | 150 ms | 30 |

